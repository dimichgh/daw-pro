{
  "name": "daw-pro-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing DAW Pro to AI agents: bridges to the app's control WebSocket and calls Anthropic/OpenAI/Suno directly for generation tasks.",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "bin": {
    "daw-pro-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "build:test": "tsc -p tsconfig.test.json",
    "bundle:plugin": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node22 --outfile=../claude-plugin/server/index.mjs --banner:js='// GENERATED FILE -- do not edit directly. Regenerate with: cd mcp-server && npm run bundle:plugin'",
    "test": "npm run build && npm run build:test && node --test \"dist-test/test/*.test.js\""
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.111.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "openai": "^6.46.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.3"
  }
}
