# Roadmap

Work top-to-bottom; `/dev-cycle` picks the first unchecked item. Check items off as they land (with tests). Keep milestones honest — an item isn't done until it works in the running app **and** over the control protocol.

## M0 — Foundation ✅
- [x] Repo scaffold, docs, agent fleet, skills
- [x] SPM monorepo: DAWCore / DAWEngine / DAWControl / AIServices / DAWApp
- [x] Domain model v0 (project, tracks, clips, transport, mixer state)
- [x] Control WebSocket server skeleton + command router
- [x] MCP server skeleton with core tool surface
- [x] App shell: dark theme, transport bar, glowing meter component

## M1 — Core audio engine ✅
- [x] Audio file import (wav/aiff/mp3/m4a) onto tracks
- [x] Sample-accurate multitrack playback via engine graph
- [x] Transport: play/stop/seek/loop region, position broadcast to UI + control clients
- [x] Per-track and master metering (peak/RMS) driving the glow meters
- [x] Master volume, per-track volume/pan/mute/solo wired to engine
- [x] Offline render (mixdown to wav) — `render.mixdown` command
- [x] Engine test harness: offline-render assertions (null test, gain test) — determinism null (exact 0.0) + unity-vs-source + gain suite; golden-master null noted as future addition

## M2 — Recording & project persistence
- [x] Record from default input onto armed tracks (`track.setArm`, `transport.record`, host-time-aligned takes) — no round-trip latency compensation yet (M4 PDC)
- [x] Input device enumeration/selection (`input.listDevices`, `input.setDevice`) — pinned-device capture fixed 2026-07-05 (benign config-change triage + first-buffer watchdog; live E2E green for default-uid pin, ZoomAudioDevice pin, and unpinned)
- [x] Punch in/out windows (`transport.setPunch`, writer accept-window)
- [x] Metronome/click with count-in
- [x] `.dawproj` bundle format: versioned JSON + media dir; open/save/autosave
- [x] Undo/redo across all commands (coalescing snapshot journal in DAWCore; `edit.undo`/`edit.redo`, Edit-menu ⌘Z/⇧⌘Z)

## M3 — MIDI & instruments
- [x] (i) MIDI domain + commands — `Clip.notes` payload (`MIDINote`: pitch/velocity/startBeat/lengthBeats, clamped, canonically ordered, mutually exclusive with audio media); `clip.addMIDI` / `clip.setNotes` / `clip.remove` on the control protocol (whole-array note set, per-clip undo coalescing); additive v1 persistence; instrument-track-only rule
- [x] (ii) Piano-roll editor (Simple + Pro views) — note draw/move/resize/velocity, snap grid, driving `clip.setNotes`; timeline clip lanes + `debug.captureUI` self-render verification
- [x] (iii) Sample-accurate MIDI scheduler — settles the sequencer clock decision (one `AVAudioSourceNode` per instrument track; host-delta live epoch / latched offline epoch), lock-free event delivery via an immutable schedule behind a C11 atomic pointer (`CAtomics` shim, ≥1 s retire bin), all-notes-off flush flag on every stop/seek/tempo/loop-wrap/reschedule, `InstrumentRendering` seam (AU-shaped for M3 vi) with EventCaptureInstrument + TestToneInstrument; validated by frame-exact offline event-timestamp tests
- [x] (iv) Built-in poly synth — 16-voice subtractive synth (PolyBLEP saw/square, triangle, sine; linear ADSR; Simper SVF low-pass) behind `InstrumentRendering`; `Track.instrument: InstrumentDescriptor?` (additive, nil ⇒ polySynth default), `track.setInstrument` control command + `track_set_instrument` MCP tool with partial-update semantics; RT-safe in-place param changes (atomic POD publish, held notes survive), kind switch = node rebuild + reschedule
- [x] (v) Built-in sampler — key zones (`SamplerZone`: audio file, root pitch, span, gain) under the same `InstrumentDescriptor`; pitched playback via linear-interp resampling (2^(Δsemitones/12) × rate ratio), one-shot mode, anti-click attack/release; zones structural (rebuild) vs scalars in-place (atomic); zone media joins the `.dawproj` bundle copy (relative `media/` refs, dedupe, `mediaFilesCopied`); `sampler` object on `track.setInstrument` + MCP
- [x] (vi-a) AU instrument hosting (headless) — `HostedAUInstrument` adapter behind the `InstrumentRendering` seam (our scheduler stays the only clock; events via `scheduleMIDIEventBlock` at in-quantum offsets; flush → CC123+120), `AUHostRegistry` (timeout-raced async prepare, strict-select/graceful-load: missing AU → silent placeholder + `status`), descriptor kind `audioUnit` with component identity + fullState base64 in `.dawproj`, `instrument.listAudioUnits` + `track.setInstrument audioUnit:` + MCP; `renderMixdown` now async (offline renders instantiate fresh AU instances); out-of-process AUv3 reliability deferred to M9 app bundling
- [x] (vi-b) AU plugin UI windows — `requestViewController`/v2 view hosting in floating glass-chrome windows; open/close over the control protocol — split 2026-07-10 per `docs/research/design-vi-b-au-windows.md` (DESIGN SETTLED — the implementation contract; unblocked by pkg-a's real app bundle):
  - [x] (vi-b-1) Plumbing + generic body — engine accessors for the live `AUAudioUnit` (concrete-class-only boundary exception) + registry `onRelease` invalidation seam; `plugin.openUI/closeUI/listOpenUIs` through a typed async `PluginUIControlling` seam (headless-honest errors); `PluginWindowLedger` (DAWAppKit, headless) + `PluginWindowManager`/`NSPanel` glass chrome (cyan/neutral, never violet); `AUGenericViewController` body only; `debug.captureUI {target:"plugin"}`; mixer/instrument open-window buttons; 3 MCP tools; headless suites + orchestrator live gate on system AUs (DLSMusicDevice/AUDelay), fully provable on the bare binary
  - [x] (vi-b-2) Vendor views — full resolution ladder (`requestViewController` w/ 5 s once-gate timeout race → v2 `kAudioUnitProperty_CocoaUI` via `AUAudioUnitV2Bridge` + `AUViewProbe` in DAWEngine → `AUGenericViewController` fallback, never-failing), `BodyHostViewController` wrapping the vendor `AUCocoaUIBase` view, v3 `preferredContentSize` sizing + `frameDidChangeNotification` resize-follow (reentrancy-guarded, clamped in-bounds); §10 de-risk spike resolved by inventory (NO AUv3 installed — the out-of-process AUv3 custom-view leg is SKIPPED-recorded, never faked); live gate on system units proves both branches — DLSMusicDevice/AUDelay → `body:"custom"` (real vendor views inside our chrome), AUMatrixReverb → `body:"generic"` — bare binary AND `dist/DAWPro.app` bundle identity; `docs/PACKAGING.md` note
- [x] (vii) MIDI hardware input — CoreMIDI UMP protocol-1.0 port, omni-connect + hot-plug; live thru on armed instrument tracks via per-renderer SPSC rings behind an atomic fanout (offset-0 events, works while stopped, drop-newest + flush-on-drop so notes can't stick); recording shares the audio take's `PlaybackAnchor` beat math and lands MIDI clips in the same "Record Take N" undo step (punch trims audio only, v0); `midi.listInputs` + snapshot `midiInputs`/`midiEventCount` + MCP; hardware-free verification via virtual sources (`scripts/e2e-midi-record.swift`)

## M4 — Mixer & FX suite
- [x] (i) Bus routing & sends — bus tracks become real mix destinations: track output routing (master or bus), per-track sends (pre/post fader, level) into buses, bus volume/pan/mute/solo/meters in the engine graph + offline render parity; `track.setOutput` / `track.addSend|setSend|removeSend` commands + MCP + tests — **COMPLETE 2026-07-06**: the parked mid-play send-activation defect resolved (stale render-clock anchor after the rewire bounce, one-flag fix — see the RESOLVED entry under ## Blocked); live bus E2E 19/19 (a send added mid-play meters on its bus with NO transport command: within ~0.3 s for audio material, at the next note-on/loop wrap for MIDI material per the settled no-chase contract)
- [x] (ii) FX insert-chain architecture — `EffectRendering` seam + `ChainHostAU` (in-process registered AU `aufx/dwch/DAWP`, spike-proven from the SPM binary) as a permanent per-strip insert point; chain edits are atomically published snapshots, NEVER graph mutations (the M4 (i) failure class eliminated by construction — verified live: mid-play add/bypass/reorder/param with zero transport interruption); pre-fader inserts on audio/instrument/bus strips; `fx.add|remove|reorder|setBypass|setParam|describe` + 6 MCP tools; GainEffect proves the seam (bit-exact chain math); PDC-ready latency reporting (0 in v0)
- [x] (iii) Built-in FX pack 1 (dynamics & tone) — 4-band parametric EQ (RBJ biquads, TDF2 Float64, 0 dB bands bit-exact-skipped), soft-knee stereo-linked compressor (real peak detector, log-domain gain computer), 5 ms-lookahead brickwall limiter (monotonic-deque window max; `|out| ≤ ceiling` for EVERY sample by construction; first nonzero `latencySamples`, plumbed engine→store→wire per-effect); spectrum display deferred to mixer UI (vi)
- [x] (iv) Built-in FX pack 2 (space & color) — Freeverb reverb (8 damped combs + 4 allpasses/channel, pre-delay, mid/side width), stereo delay (integer lines, echo at exactly round(timeMs×rate/1000), feedback high-cut, ping-pong crossfeed), tanh saturator (−driveDb/2 dB fixed compensation, odd harmonics at H2/H3≈1e-15), stereo-linked gate (bit-exact passthrough open, true zeros closed, attack/hold/release ramps), 2-voice chorus (90° stereo / 180° inter-voice sine LFO offsets); all atomic-POD-publish RT-safe; 9 kinds on `fx.describe` + MCP; verified live (delay echoes in bounce, mid-play reverb add, −1 dB gate silence, 5-kind persistence)
- [x] (v) AU effect hosting (headless) — `HostedAUEffect` pull-render adapter behind `EffectRendering` (in-place via stash box + preallocated scratch ABL, render failure → dry passthrough); registry reuses the instrument timeout-raced prepare with strict-select on add / placeholder+warning on load; async prepare → single-strip invalidate → chain republish (never a graph rebuild); real `latencySamples` from `au.latency` (AUPeakLimiter: 96 @ 48 kHz); fullState in `.dawproj`; `fx.add kind=audioUnit` + `fx.listAudioUnits` + MCP (47 tools); found+fixed AUv2-bridge input-bus-disabled bug; plugin UI windows stay gated on M9 bundling
- [x] (vi) Full mixer view — console-style horizontal rack: channel strips (kind badge, inserts w/ bypass dots, sends w/ mini-faders, output picker, pan knob, long-throw fader w/ dB readout, mute/solo/arm glow buttons, live meter), BUSES-divided bus strips, accent-bordered pinned MASTER w/ stereo meter; `MixerModel` headless view-model in DAWAppKit (strip derivation/dB/pan/geometry, 10 tests); ARRANGE⇄MIX header toggle + `ui.showMixer` debug-tier command; Canvas controls (VerticalFader/PanKnob/SendMiniFader); design language codified as the "Mixer console" pattern in DESIGN-LANGUAGE.md; verified by captureUI self-render (agent + independent) — master insert chain deferred until DAWCore grows a master effect chain; hosted-AU insert picker UI deferred (control-plane add works)
- [x] (vii-a) Automation domain model + store CRUD — `AutomationLane/Target/Point/Curve` in DAWCore (target enum persistence-stable incl. deferred sendLevel; pure `value(atBeat:)` evaluator, canonicalized points, clamp-through-target-range), `Track.automation` additive Codable, ProjectStore CRUD (idempotent addLane w/ v0 rejections, whole-array setPoints w/ 4096 cap + `automation.points:<laneID>` coalescing, cascades drop targeting lanes inside removeEffect/removeSend/bus-delete edits), `.dawproj` persistence omitted-when-empty (pre-automation saves byte-identical); 19 tests
- [x] (vii-b) Engine read path: volume/pan lanes — `AutomationSchedule`/`AutomationRenderer` (atomic slot + ≥1s retire bin, MIDIEventSchedule pattern; preallocated cursors w/ binary-search reseek), post-chain-walk gain+pan stage in ChainHostAU + InstrumentRenderer (per-sample linear ramps — analytic bounce error 7.4e-9; pan-center bit-exact null), fader-override (mixer pinned, lane supplies gain; mute/solo gating intact) + stopped-WYSIWYG, no-restart point republish (proven: MIDI schedule object identity unchanged across mid-play edits), live/offline parity cross-diff 0.0; 9 engine tests
- [x] (vii-c) Engine read path: built-in FX-param lanes — per-quantum render-thread POD stores (quantum-start eval, `AutomationParamOverlay` base/effective split w/ knob-revert on lane removal, 64-track/strip cap matching preallocated cursors); lock-free-POD audit per kind (all nine: main-actor apply allocates → lean render-thread setter added; derive paths pure libm/arithmetic, limiter lookahead fixed-size, delay/reverb/chorus lines MAX-sized read-offset changes)
- [x] (vii-d) Control protocol + MCP — `automation.addLane|removeLane|setPoints|setLaneEnabled` (target/point wire shapes round-trip through the model's own Codable so wire never drifts from persistence; per-index point parse errors; store rejections surface verbatim), snapshot lanes ride Track's encoding for free, 12 control tests, 4 MCP tools (51 total) with AI-agent-oriented descriptions
- [x] (vii-e) Arrange automation lane UI (v0) — per-track disclosure row (cyan-glow glyph when lanes active), VOL/PAN target picker + green enable toggle + delete, Canvas breakpoint editor (neon polyline w/ glow, click-add, drag-move w/ live readout, double-click/⌫ delete, dimmed when disabled; pan lanes neutral-white per mixer convention); pure geometry/edit/selection logic headless in `DAWAppKit.AutomationLaneModel` (16 tests); all edits via vii-a store methods (per-tick drag commits ride the `automation.points:<laneID>` coalescing key); debug-tier `ui.showAutomation` for headless capture; pattern codified in DESIGN-LANGUAGE.md — **automation v0 complete** (vii-f deferred)
- [ ] (vii-f) DEFERRED post-v0: touch/latch/write recording, sendLevel engine support, AU-param lanes, master lanes, bezier curves
- [x] (viii-a) PDC plan math (DAWCore) — `LatencyCompensation.swift`: `PDCPlan.compute(input:cap:)` (`PDCStripKind.track(outputsToMaster:hasSends:)|.bus`, per-strip plannedSamples/compensationSamples/clamped/skewSamples + residual, T/B/maxPathLatency; bypass-stable maxima over all-effects sums, comp from active sums, sends pin to T, no-sends direct-to-master refinement to T+B, skew=B only for direct+sends while B>0, 16384 cap w/ honest unclamped reporting, hostile negative inputs clamped not precondition-crashed); 15 unit tests incl. spec worked example verbatim
- [x] (viii-b) Compensation ring (DAWEngine) — `CompensationDelay.swift` (`CompensationDelayState`: 32768-float pow-2 rings/channel, heap atomic target+resetFlag, render-only writeIndex/appliedOffset/historyClean; clean-history target-0 fast path provably buffer-untouched; dirty target-0 keeps writing so later retargets crossfade into real history); inserted in ChainHostAU between chain walk and automation.apply + InstrumentRenderer main AND idle paths; `PlaybackGraph.setCompensationTarget(_:forStrip:)` + `armCompensationResets()` at stopAllPlayers/startAllPlayers (covers cold start + offline start; v0 loop wrap flushes BY DESIGN — wrap is a restart where chain tails already cut; spec's no-flush rule applies when seamless wrap lands); 7 tests: impulse alignment 4800/5040→single coherent 0.5 peak, exact null, 512-bit-pattern inertness, bit-exact crossfade formula, reset kills 240-sample stale tail, determinism, instrument-path 480
- [x] (viii-c) PDC recompute wiring + reporting — single choke point at the tail of `PlaybackGraph.applyParameters` (all 6 trigger events already funnel through it; offline calls it both sides of engine start → parity by construction); `EffectChainState.latencySamplesAllEffects`; `PDCReport` (outputLatency structured for a future master chain); additive snapshot fields (per-strip chainLatencySamples/compensationSamples always, clamped/skew omit-if-false/zero; project `pdc` object); no new command (spec §6, snapshots ride existing surface; MCP passthrough untyped so no TS change); 7 wiring tests + live E2E 23/23 (bypass mid-play: totals stable, one-strip retarget, playback never stops)
- [x] (viii-d) PDC parity — latent-chain + shared-send-bus project cross-diffed across two offline renders at bit-level 0.0 with plan-correct 4-path coherent alignment (peak exactly 1.0); live-capture harness doesn't exist yet, so v0 bar = offline determinism + live E2E assertions (honest note); ARCHITECTURE settled entry landed at design time; strip latency-readout DESIGN note deferred to when the mixer grows the readout — **M4 (viii) PDC v0 COMPLETE**

## M5 — Editing depth
- [x] (i-a) Clip edit domain + store — `Clip.gainDb` (−72…+24) + `fadeInBeats`/`fadeOutBeats` + `FadeCurve.linear|equalPower` + NEW `startOffsetSeconds` (didn't exist; split/trim impossible without it), all additive omit-when-default Codable incl. ClipDocument mirror (pre-edit projects byte-identical); pure `envelopeGain(atBeat:)` (equal-power sin/cos quarter-cycle, gain×fades multiplicative — midpoints 0.5 / 0.7071 / −6dB≈0.5012 verified); store ops splitClip (fixed-tempo offset math + MIDI partition w/ overhang truncation + fade redistribution), trimClip (leading edge advances offset, drops outside notes, re-clamps fades), moveClip, setClipGain, setClipFades — clamped, undo-coalesced (`clip.trim|move|gain|fades:<id>`), proportional fade reduction when in+out > length; 17 tests / 3 suites
- [x] (i-b) Clip gain/fade engine read path — settled design implemented: `ClipFadeBake.swift` (piecePlan contiguity by construction, applyEnvelope from `Clip.envelopeGain` w/ gain zeroed, bake-owned second AVAudioFile reader — never the player's stateful streaming instance); scheduleAll three-piece buffer/segment/buffer (unfaded = pre-i-b byte-identical path; bake failure → unfaded fallback, timing over shape), startOffsetSeconds honored (bit-exact), gain = player.volume every applyParameters pass + in-place `clipGainChanged` (no restart; gainDb in NO reconcile signature; fades/offset ARE schedule-affecting → restart+re-bake); MIDI clips unreachable by construction; 12 tests: fade error 0.0, null 0/96000 frames, splice-invariance residue 2.98e-8 (1 ulp), equal-power ≤ +3.02 dB, mid-fade start exact, determinism
- [x] (i-c) Clip edit control + MCP — `clip.split|trim|move|setGain|setFades` (split returns both clips; parseFadeCurve names the offending field; store rejections verbatim), 5 MCP tools (56 total), 14 control tests, live E2E 24/24 (split note partition, snapshot round-trips, persistence, error surfaces) re-run independently ALL PASS
- [x] (i-d) Waveform editor UI — interactive ClipBlock (body drag = snapped move, ~6pt edge zones = snapped trim, double-click/context-menu split incl. at-playhead, corner fade grips w/ translucent linear/equal-power shading + alt-click curve toggle via context menu, cyan dB gain chip w/ vertical drag), SNAP header control (Off/Bar/Beat/1/2/1/4 — Bar follows the meter, hence new ClipSnap not the piano roll's fixed-bar SnapResolution), `ClipWaveform` cached off-main peak outline windowed by startOffsetSeconds; headless `ClipEditModel` in DAWAppKit (snap/hit/preview math mirroring store clamps, 14 tests); all edits via the five store methods; capture-verified (split visible, fades bowed, waveform continuous across split, −6.0 dB chips) — **M5 (i) waveform editor COMPLETE** (44 tests total across i-a…d)
- [x] (ii-a) Time-stretch library + seam design — signalsmith-stretch (MIT) DECIDED (research doc 2026-07-05; Rubber Band commercial fallback, Apple TimePitch = future RT preview only); integration seam SETTLED (ARCHITECTURE entry + scratchpad stretch-seam-spec.md): 3 additive clip fields, absolute ratio v0, full-source-file content-keyed CAF cache outside .dawproj (geometry-free keys — trim/split/move/tempo never invalidate), offset×ratio schedule mapping w/ fade bake unchanged, debounced latest-wins async render w/ silence-while-pending, identity = exact bypass
- [x] (ii-b) Vendor + facade — signalsmith-stretch v1.3.2 (57b93f4) + signalsmith-linear (7f53cdd) vendored verbatim w/ licenses + VENDORED.md pins; `CSignalsmithStretch` target (flat-C nullability-annotated API, one C++ TU, Accelerate backend, package cxx17 — no conflicts); `OfflineStretcher` facade (planar core + PCM-buffer convenience, upstream `exact()` recipe via output_seek → no post-trim, FIXED RNG seed → determinism + content-keyed-cache-ready, `isIdentity` bypass predicate excludes formant per seam rule); 8 tests: lengths EXACT (144000/72000/96000), pitch 440→440.00 / +7st→659.00 vs 659.26, formant variant, cancellation at poll 2 of ~36, bit-identical runs; FLAG for ii-d/e: 0.75× overlap-add overshoot peak 1.414 from 0.8 input (~+4.9 dB) — Float32 CAF won't clip on disk but headroom decision needed at listening spike
- [x] (ii-c) Stretch domain + surface — `Clip.stretchRatio/pitchShiftSemitones/formantPreserve` (omit-when-default + ClipDocument mirror; pure `sourceWindowSeconds(tempoBPM:)` + `isStretchIdentity` helpers), store ops `setClipStretch` (nil=keep-current, MIDI-rejected, `clip.stretch:<id>` coalesce) + `stretchClip(toLengthBeats:)` compound (window-invariant ratio scaling; ratio-clamp re-derives length so invariance survives; fades re-clamp) + split copies to both halves + trim preserves; `clip.setStretch|stretchToLength` commands + 2 MCP tools (58 total); 20 tests/+4 suites; live wire smoke PASS (clamp to 4, compound 8 beats→ratio 4, MIDI rejection verbatim, snapshot round-trip)
- [x] (ii-d) Stretch engine wire — `StretchRenderCache` (@MainActor; SHA256 content key over path/size/mtime/param-bits/engine-version, `<key>.caf` Float32 full-file renders in `~/Library/Caches/DAWPro/StretchRenders/`, 250ms debounce + per-clip latest-wins cancel + same-key single-flight, unique `.partial-<token>.caf` → atomic rename); PlaybackGraph `StretchResolution` resolver seam (ready → rendered CAF replaces source w/ `startOffsetSeconds × ratio` offset mapping, pending → silence, identity clips never touch the resolver — null test 0.0); AudioEngine owns cache + `stretchDesired` dedupe (failed renders not retried on identical params — re-edit retries), completion invalidates + restarts via `tracksDidChange`; `renderMixdown` awaits pending renders (WYSIWYG); OfflineRenderer `stretchedFileProvider` seam (nil → silent, never wrong-speed); snapshot `stretchRendering`/`stretchError` omit-when-default; 8 tests (identity null 0.0, cache hit/miss/param/mtime re-key, offset map bit-exact at frame 48000, stretched fades 0.0, latest-wins single CAF, silent-pending) + 16-check live E2E re-run independently ALL PASS (transient rendering flag, stretched window audible past source end, mid-play re-stretch resumes through restart seam) — 680/79. Deferred: cache LRU eviction sweep
- [x] (ii-e) Stretch UI + listening spike — ClipBlock ⌥-drag right-edge stretch handle (high-priority `.modifiers(.option)` gesture, live "length · ratio" readout, "≈" grip hover cue via hover-scoped NSEvent flags monitor), amber out-of-band (outside 0.75–1.5×) border/badge glow, persistent SF Mono ratio/pitch badge on non-identity clips, `ClipShimmer` animated sweep while rendering (10 Hz bounded-window AppModel poll over pull-based `clipStretchStatus`, kicked by `onChange(of: stretchSignature)` so gesture AND control edits shimmer), red border + error dot on failure; headless `ClipStretchModel` in DAWAppKit (stretchPreview mirrors store's window-invariance + clamp→length re-derive, cross-checked against the real store; 14 tests → 694/80); DESIGN-LANGUAGE patterns recorded; capture-verified (badges, amber tint, shimmer frames). Listening grid (ACE-Step absent → live-app polySynth phrase, chords+staccato): 36 cells, true peaks −7.67…−1.69 dBTP, 0/36 over 0 dBFS, lengths exact, ii-b sine overshoot did NOT reproduce on musical material (0.75× cell peaks 1.5 dB BELOW source); headroom call: no baked compensation gain — Float32 end-to-end, master-bus true-peak stage later, amber hint surfaces risk; **signalsmith-stretch stamped PROVISIONAL-FINAL for 0.75–1.5× (no Rubber Band escalation), human audition pending** (scratchpad stretch-listen/LISTENING-NOTES.md) — **M5 (ii) time-stretch COMPLETE**
- [x] (iii-a) Take/comp domain + store — `Takes.swift` (TakeLane/CompSegment/TakeGroup w/ derived rangeBeats + clamped crossfadeSeconds, pure deterministic CompFlattener), `Clip.takeGroupID` + `Track.takeGroups` (omit-when-default/empty additive Codable + ProjectDocument mirrors, lane payload = ClipDocument w/ media ref), 7 store ops in `ProjectStore+Takes.swift` (groupTakes connected-cluster overlap, setCompSegments/selectTake coalesce `take.comp:<gid>`, removeTakeLane, flattenTakeGroup→[Clip], moveTakeGroup, setTakeCrossfade) + shared `requireNotCompMember` guard in all 9 clip-edit ops (quantize ops add 1 call each when they land) + setTempo re-flatten in one performEdit + planMedia walks lane media deduped; 27 tests/+4 suites → 721/84, 0 warnings, independently verified (design: ARCHITECTURE settled entry + comping-quantize-spec.md)
- [x] (iii-b) Take control + MCP + recording auto-group — 7 `take.*` commands in `Sources/DAWControl/Commands.swift` (`take.group/setComp/select/removeLane/flatten/move/setCrossfade`, params round-tripped through the model's own Codable — `CompSegment` arrays esp., per-index parse errors, store rejections verbatim) + 7 matching MCP tools in `mcp-server/src/index.ts` (65 total) with comping-model-teaching descriptions; `project.snapshot` carries `track.takeGroups`/`clip.takeGroupID` for free (iii-a Codable); recording auto-group (`ProjectStore.autoGroupRecordedTake`, called from `finishTake`, audio-only v0, one `performEdit` — new lane on an existing group's range / groupTakes-semantics group-from-plain-clips / disjoint stays a plain clip, newest always wins the default comp; MIDI unaffected; loop-cycle recording explicitly deferred); 18 control tests + 5 headless auto-group tests → 744/86, 0 warnings; live E2E 34/34 (3 overlapping MIDI clips → group → snapshot → clip.trim member rejection verbatim → setComp 2 segments → snapshot → select → flatten → trim succeeds → undo/redo round-trip)
- [x] (iii-c) Take lane UI — headless `DAWAppKit.TakeLaneModel` (TakeLaneGeometry beat↔x/row-stacking/segment rects, TakeComp paint-drag classify+snap+merge+normalize kept provably inside setCompSegments' acceptance, TakeLaneSelection disclosure/badge/splice logic; 20 tests incl. store-validation cross-check); `TakeLanesView` expanded region (per-group header w/ name+count+FLATTEN, dim lane sub-rows w/ mini waveform/MIDI pills, glowing comp highlights, newest-lane accent, paint/select drag w/ 0.15-beat click threshold riding `take.comp:<id>` coalescing, per-lane context menu) above the automation row; ClipBlock take chrome (bottom-center "name · N" badge, glowing splice hairline at internal comp boundaries, take-focused context menu); TrackListView stacked-layers glyph (signal-green when groups exist) + height-matched `TakeTrackControls` sidebar; `ui.showTakes` debug command (ui.showAutomation pattern); crossfade control skipped v0 (spec-optional); DESIGN-LANGUAGE "Take lanes" entry; 764/88, 0 warnings, 4 captures verified (collapsed badge / expanded default / 3-segment painted comp / post-flatten)
- [x] (iii-d) MIDI quantize — pure `Quantize.swift` (`QuantizeSettings` gridBeats/strength 0–1/swingPercent 50–75 MPC/quantizeEnds; `QuantizeTarget.nearest` + `slotOffset` groove seam — iii-g adds an additive `groove` field + one branch, wire shape fixed; `MIDIQuantizer` lerps onsets toward swung targets, lengths preserved unless quantizeEnds, re-canonicalized, deterministic + idempotent at strength 1; spec formula `(2·swing/100−1)·grid` offbeat delay applied BEFORE lerp), `quantizeClipNotes` (coalesce `clip.quantize:<id>`, audio clips rejected verbatim pointing at future clip.quantizeAudio, comp members rejected via shared guard), `clip.quantize` command (flat validated scalars per setFades/setStretch house style) + `clip_quantize` MCP (66 total); 12+5 tests → 781/92, 0 warnings; live E2E ALL PASS re-run independently (strength 1 exact, 0.5 halfway, swing 66 → 1.32/3.32, audio-reject verbatim, undo restores)
- [x] (iii-e) Transient detection — vDSP spectral-flux `TransientAnalyzer` (1024 Hann/256 hop, half-wave-rectified flux, 0.35 s moving-median threshold × k(sensitivity 0→3.0…1→1.2) + 1% peak-flux floor, 30 ms peak-pick, 64-sample energy-rise refinement → `[TransientMarker]` timeSeconds+strength 0–1 normalized, deterministic, offline-only) + `TransientCache` JSON sidecars in `~/Library/Caches/DAWPro/TransientMaps/` (SHA256 path/size/mtime/sensitivity-quantized-0.05/analyzerVersion first-16-hex, partial+atomic-rename, single-flight, corrupt→self-heal, `analysisCount` spy); additive `detectTransients(inFileAt:sensitivity:)` on `AudioEngineControlling` (default `[]` keeps fakes green) + `detectClipTransients` store op (window-filter + beat-map through tempo/stretch, MIDI rejected verbatim, re-locate after await); `clip.detectTransients` command + `clip_detect_transients` MCP (67 total); 14 engine + 7 control tests → 802/94, 0 warnings; measured: click-train max onset error 0.67 ms (bar 5 ms), decaying-pad zero tail onsets; live E2E ALL PASS (MIDI-mixdown clicks maxErr 12 ms incl. synth attack ramp, 0 spurious ≥ 5% strength, cache rerun identical, MIDI-reject verbatim)
- [x] (iii-f) Audio quantize v0 — pure `AudioQuantizePlan` (slice at transients → monotone strength-lerp nudge to `QuantizeTarget.nearest` grid → natural-length slices reading only their own `[onsetᵢ,onsetᵢ₊₁)` span, so a moved slice NEVER replays a neighbour's attack; representational equal-power crossfades at COMPRESSED joins via the `CompFlattener` pattern, clean-cut silence at EXPANDED gaps — deviation from spec's literal source-continuation gap-fill, which doubled transients; no per-slice stretch) + `AudioQuantizeSettings` (grid/strength/swing/sensitivity/crossfade/minSlice); `applyAudioQuantize` sync (replace clip w/ slices in ONE `performEdit("Quantize Audio")`, no coalesce) + async `quantizeAudioClip` (detect → re-locate → apply); rejections verbatim: MIDI (`quantizeRequiresAudioClip`), non-identity stretch (`audioQuantizeStretchUnsupported`), comp member (`clipInTakeGroup`), <2 usable transients (`audioQuantizeNoTransients`), headless (`engineUnavailable`); `clip.quantizeAudio` command + `clip_quantize_audio` MCP (68 total); 8 plan + 8 store + 7 control tests → 825/99, 0 warnings; offline-render grid-alignment E2E ALL PASS (off-grid clicks max dev 0.1013 beat → on-grid bounce dev 0.0000 beat, 6/6 onsets, no ghosts; undo→1 original clip, redo→7 slices; MIDI+stretch reject verbatim)
- [x] (iii-g) Groove templates — pure `Groove.swift` (`GrooveTemplate {gridBeats, cycleBeats, offsets}` Identifiable/Codable/Sendable, init normalizes offset count to round(cycle/grid) + clamps each to ±grid/2 closed bound; `extract(fromOnsetBeats:…)` nearest-slot snap → per-folded-slot deviations average → empty slots 0; `offset(forSlot:)` proper-modulo fold; `builtin(named:)` swing8/16:54–75 grid 0.5/0.25 cycle 2·grid offsets `[0,(2P/100−1)·grid]` + 8 canonical `builtinNames`, deterministic FNV ids); one additive `groove` field on `QuantizeSettings`+`AudioQuantizeSettings` (forwarded via `targetSettings`) and ONE branch in `QuantizeTarget.slotOffset` (groove wins over swing) — applied through BOTH quantize paths unchanged; project-level `ProjectStore.grooveTemplates` (published, additive `EditState` field → undo covers add/remove, additive optional `ProjectDocument`/`ProjectSnapshot` omit-when-empty → pre-groove byte-identical, no schemaVersion bump); store ops `extractGroove` (MIDI onsets / audio via detectClipTransients, clip-relative)/`removeGrooveTemplate`/`resolveGroove` (builtin→id→name precedence); `groove.extract|list|remove` commands + `groove?` param on `clip.quantize`/`clip.quantizeAudio` (field-named unknown-ref error) + 3 MCP tools + groove param on both quantize tools (71 total); 17 core + 8 control tests → 850/105, 0 warnings; live E2E ALL PASS (swung MIDI extract offsets [0,0.16] exact; saved groove via clip.quantize onsets on targets dev 0.000000; builtin swing8:66 via clip.quantizeAudio → bounce onsets on groove targets max dev 0.0013 beat; groove.list 8 builtins + saved; remove+undo restores; unknown-ref verbatim)
- [x] (iv-a) Loudness core (DAWCore) — pure `Loudness.swift`: BS.1770-4 K-weighting (analog-prototype constants re-derived at any rate, 48 k table pin ≤ 1e-6), 400 ms/75 % gated integrated (−70 abs → −10 LU rel), max momentary/short-term (3 s/0.1 s hop, same 100 ms energy series), 4× polyphase true peak (Annex 2; odd-length 129-tap Kaiser-β6 windowed sinc → phase 0 ≡ original samples + 3×32-tap phases, per-phase unity DC, passband ≤ 0.01 dB dev to 20 kHz at 48 k); `RenderedAudio` moved DAWEngine→DAWCore + in-place `applyGain(linear:)` (mixdown/render suites frozen); `LoudnessMeasurement` Codable (nil = silence, silence encodes `{}`, no −inf on the wire); 21 tests → 871/112, 0 warnings; measured: 997 Hz −23 → −22.99998, −33 → −32.99998, left-only → −26.0103, gate-drop → −23.031, 44.1 k cal → −22.9972, EBU 3341 fs/4+45° TP +0.098 dBTP (sample peak −3.0103), applyGain −6 dB shift −6.0000005, −80 dBFS → nil integrated (spec: scratchpad stem-lufs-spec.md §3)
- [x] (iv-b) Buffer-render seam + measured/normalized bounce — additive `renderOffline`/`offlineCompensationTargets`/`writeAudioFile` on `AudioEngineControlling` (defaults keep fakes green — legacy fakes compile unchanged, default renderOffline/write refuse readably, targets `[:]`); `renderMixdown` refactored over them (behavior frozen — buffer bit-identical to the written file, mixdown/stretch suites untouched); `OfflineRenderer.writeWAV` split + `compensationPlanTargets` planning probe (reconcile + one parameter pass, no frames — parity with `PDCPlan.compute` asserted, limiter fixture 0/240); `ProjectStore+Render.swift` `measureLoudness`/`renderBounce` with static-gain normalize + true-peak ceiling CLAMP (no limiter v0 — honest `limitedByCeiling`; `bounceSilent` on gated-silent + target, verbatim message; output re-measured post-gain; all-clips + 2 s default window; range enforcement is iv-d wire work); `BounceResult`/`BounceLoudnessReport`/`LoudnessMeasureResult` Codable in `Loudness.swift`; 15 tests → 886/114, 0 warnings; measured: bounce −16 target → −16.0000005 LUFS re-read from the written WAV; clamp case input −19.9988 LUFS/−2.000 dBTP, target −14 ceiling −1 → gain +1.000 (not +6), output −18.9988 LUFS/−1.000 dBTP; forced PDC target 480 → onset at exactly frame 480 (spec §4.1–4.3, §5)
- [x] (iv-c) Stem plan + export — pure `StemPlan.swift` master-input partition (one stem per direct track + per bus in track-list order; send contributions fold into the DESTINATION bus stem, source stems stay dry with sends stripped; bus-routed track ids reject `stemNotMasterInput` verbatim; silent-dummy-bus solo transform parks rerouted direct-outs — present EXACTLY when needed, so the graph's missing-bus→master fallback can't leak; "NN Name.wav" sanitize/collision with " 2"/" 3" case-insensitive suffixes) + `StemFile`/`MixdownFile`/`StemExportResult` Codable; `renderStems` on `ProjectStore+Render.swift`: `offlineCompensationTargets` probed ONCE full-session → N sequential `renderOffline` passes each under those FORCED targets, memory-bounded render→measure→write (one buffer in flight), stems measured NEVER normalized, `includeMixdown` "00 Mixdown.wav" under the same targets, empty selection → `nothingToRender`; 12 plan + 5 engine tests → 903/116, 0 warnings; measured nulls: flat 5.4e-20, bus-FX 0.0 (lead dry-tail RMS 0.0 vs bus 0.108), PDC-limiter 0.0 forced vs 0.25 subset-auto-plan negative control (bar 1e-4); includeMixdown ≡ renderMixdown bit-exact (spec §1–2, §4.3)
- [x] (iv-d) Render wire + MCP + E2E — `render.measureLoudness`/`render.bounce`/`render.stems` commands (field-named validation `lufsTarget` −70…0/`truePeakCeilingDb` −20…0/`fromBeat`≥0/`durationSeconds`>0/per-index `trackIds`; store rejections `bounceSilent`/`stemNotMasterInput`/`nothingToRender`/`engineUnavailable` verbatim; responses = the model's own `LoudnessMeasureResult`/`BounceResult`/`StemExportResult` Codable) + 3 MCP tools (71→74) teaching −14 streaming/−23 EBU R128 LUFS targets, the ceiling-CLAMP-not-limiter semantics (`limitedByCeiling` honesty + built-in-limiter-then-re-bounce escape hatch), measure-vs-bounce split, stems-never-normalized + Σ-stems-≡-mixdown invariant, nil-fields-mean-gated-silence; fixed an off-by-one crash in iv-a's `Loudness.swift` short-term-window loop (`1...(hopCount−hopsPerShortTerm)` invalid when equal — hit by any exactly-30-hop/3.0 s render) found via live E2E, not a wire change; `RenderCommandTests.swift` (17 tests, dedicated `FakeRenderEngine` incl. headless-`engineUnavailable` coverage) + the shared `FakeEngine` extended with `renderOffline`/`writeAudioFile` so all three route through the existing coverage-loop test too → 920/117, 0 warnings; MCP `tools/list` stdio smoke confirms 74 tools incl. all three; live E2E ALL PASS (2-track+bus-send session; measured −14.91 LUFS/−12.36 dBTP; bounce target −16 → −16.0000 LUFS re-measured, written-file sample peak −13.4545 dBFS vs report.truePeakDbtp −13.4545 dBTP exact; peaky hot material target −6/ceiling −1 → `limitedByCeiling` true, output −1.0000 dBTP/−27.60 LUFS (target unreached, honest); lufsTarget:5 and bus-routed trackId reject verbatim; 2 stems (T1+Bus) NN-named, Σ stems vs 00 Mixdown.wav residual 0.0); export-panel UI deferred to app-chrome epic (spec §4.4, §7)

## M6 — AI suite
- [x] ACE-Step-1.5 sidecar setup — `scripts/ace-step/install.sh` (idempotent: uv-managed Python 3.12 venv, shallow upstream clone, `acestep-download` XL-turbo + XL-sft DiT + 4B LM ≈55 GB, per-model sanity incl. SHARDED layouts — first run's single-file-only check was a real bug caught by the real install) + `run.sh` (loopback 127.0.0.1:8001, MLX LM backend, `ACESTEP_PROJECT_ROOT` export — upstream's serve-time generation path builds `project_root/checkpoints` EXPLICITLY, bypassing the documented `ACESTEP_CHECKPOINTS_DIR`; without it the first generation re-downloads the whole bundle, found live; + defensive src/checkpoints symlink); `SidecarManager` actor (health probe → notInstalled/installedNotRunning/starting/healthy/error, Process spawn w/ pidfile + ~/Library/Logs/DAWPro/ace-step.log, SIGTERM→SIGKILL stop, DAWPRO_ACESTEP_DIR override, dry-run) + `ai.sidecarStatus|Start|Stop` + 3 MCP tools; 13+8 tests on stub NWListener servers; REAL GATE PASS: weights installed (55 GB), sidecar starts from the wire and reports healthy (XL-turbo DiT, pid-tracked) in <60 s (2026-07-06)
- [x] `ACEStepClient: SongGenerating` — protocol reshaped to async submit/poll (`generateSong`→submission, `generationStatus`→state/progress/audioPath; SunoClient minimally adapted, dormant); actor client against upstream's REAL routes read from source (`POST /release_task`, `POST /query_result` numeric status map + JSON-encoded nested result, `GET /v1/audio` streaming, `{data,code,error}` envelope, NO cancel route upstream = none shipped); succeeded `result.file` is a PRE-BUILT `/v1/audio?path=…` URL requested verbatim (re-wrapping double-encodes → 403 — REAL-gate-found bug the stub hid; regression test pins the exact request target); audio fetched once per job to temp WAV + cached; `ai.generateSong`/`ai.generationStatus` commands (sidecar-unreachable re-probes SidecarManager for the actionable message) + `generate_song`/`generation_status` MCP tools (79 total); stub tests + stub E2E; REAL GATE PASS: prompt + bracketed lyrics → 60 s lofi track generated in ~52 s wall on the M5 Max (LM plan 51 tok/s → tiled VAE decode) → 10.99 MB RIFF/WAVE 48 kHz on disk via the wire — **first AI-generated audio out of DAW Pro** (2026-07-06)
- [x] (iii-a) Generation→project import pipeline — succeeded-poll `metas` (bpm/duration/genres/keyscale/timesignature) + echoed `prompt` surfaced as additive omit-when-nil fields on `SongGenerationStatus` (threaded through `ACEStepClient`'s decode; nil on queued/running and when upstream omits them — genres list-joined); DAWCore `GenerationImporting` seam (`fetchGeneration(jobID:)→GeneratedSongResult`, injected as `ProjectStore.generationSource` — the MediaImporting/engine bridge pattern, so DAWCore stays AI/network-free) with DAWControl's `SongGenerationImportSource` adapter over the SAME router `SongGenerating` (the seam IS the status re-poll — audioPath already fetched-once). `ProjectStore.importGeneration(jobID:trackName:atBeat:setProjectTempo:)→(trackId,clipId,adoptedTempoBPM?)`: copies the WAV out of the volatile sidecar cache into a stable project-adjacent home (`generationImportsDirectory`, recording-take model → planMedia folds it into the bundle on save), reads authoritative on-disk duration, and in ONE `performEdit("Import Generation")` appends an AI-flagged track + AI-flagged clip (violet) at `atBeat` (default 0) with tempo adoption via the shared `applyTempoChange` (default: adopt `bpm` ONLY on an empty project; explicit true/false forces/forbids; needs bpm) — single undo restores track AND tempo; rejections verbatim (still-running→`generationNotReady` pointing at ai.generationStatus, missing cache→`generationAudioMissing`, unknown/expired→client's `jobNotFound`). `ai.importGeneration` command + `import_generation` MCP tool (79→80, teaches generate→poll→import→arrange). 981 tests/124 suites (was 961/123), 0 warnings; stub-sidecar E2E 32/32 (generate→poll succeeded bpm 92→import→snapshot AI track+clip+tempo 92 on empty project→undo restores both→setProjectTempo:false on now-non-empty project leaves tempo unchanged (job-2 bpm 140 NOT adopted)→still-running + unknown-job errors surface). Real-sidecar path proven by M6 (ii); stub E2E is the exercise bar here
- [x] (iii-b) Sketchpad UI — headless `DAWAppKit.SketchpadModel` (the whole generate→poll→import state machine against an injected `SongGenerating` + store-import closure: `SketchpadCandidate` lifecycle queued/running(progress,statusText)/succeeded(audioPath,bpm?,duration?)/failed(message)/imported(trackID,trackName), `generate()` submit→append, `refresh()` polls only active candidates with **failed-poll tolerance** — a thrown poll marks the card `isStale` and keeps polling, never kills it; only a returned `.failed` state fails a card — `importCandidate` via the closure, the sidecar-status→banner+`canGenerate` surface, the `[verse]/[chorus]/[bridge]/[outro]` section helper, and the 15–240 s duration clamp; 21 tests against a scripted fake `SongGenerating`). `SketchpadView` docks a 340 pt violet-edged glass panel on the right of Arrange (violet everywhere AI: header, section buttons, GENERATE glow, candidate accents, IMPORT + imported `checkmark.seal` badge; cyan PREVIEW/length readouts; red failed; amber sidecar/RECONNECTING): prompt/lyrics TextEditors + section-tag buttons, a −/＋ length stepper, the sidecar banner with an inline START, and candidate rows with the `ClipShimmer`-recipe generating sweep + SF Mono `STEP/％`/`BPM·LEN` readouts + an app-layer `AVAudioPlayer` preview (off the engine, one-at-a-time) + one-click IMPORT → `ProjectStore.importGeneration` (violet clip + tempo adoption, one undo). Poll timer (1 s, active only while generating) + preview live in the view; header SKETCHPAD toggle + `ui.showSketchpad` debug command + debug-only seed/drive commands (`debug.sketchpadDemo/Generate/Refresh/Import/State/Reset`, off `allCommands`/MCP per house pattern). 1002 tests/125 suites (was 981/124), 0 warnings; **4 stub-sidecar captures agent-verified** (empty composer w/ disabled GENERATE; real stub generate→succeeded row w/ 92 BPM + PREVIEW/IMPORT; import→violet IMPORTED badge + new violet AI clip in the timeline + tempo adopted 92; demo state-variety = generating shimmer/40% + red failed). DESIGN-LANGUAGE "Sketchpad" entry added; no MCP/ARCHITECTURE change
- [x] (iii-c) Stems: upstream `task_type` extract + lego mapped through `ai.extractStems`/`ai.legoGenerate`/`ai.importGeneratedStems` + 3 MCP tools (80→83) — upstream facts read from source and they CORRECTED the scout: extract AND lego are single-track-per-job (one `track_name` per `/release_task`) and BOTH require `src_audio_path` (lego = new track conditioned on existing context audio, not from-scratch); client fans out N sub-jobs under a composite `"stems:<uuid>"` id polled through the SAME `generationStatus` (aggregated progress, any-sub-job-failure fails the composite naming the track), additive `SongGenerationStatus.stems: [StemResult]?`; source-audio upload per `release_task_audio_paths.py` allowlist = stage a COPY under the shared `$TMPDIR`; `importGeneratedStems` = all-or-nothing stem staging then ONE performEdit → N violet AI tracks+clips at atBeat, tempo adoption iii-a rule from first bpm-bearing stem, SINGLE undo removes all + restores tempo; errors mirror iii-a verbatim. 1031 tests/127 suites (was 1002/125), 0 warnings, npm clean at 83; stub-sidecar E2E vs the real app binary 27/27 (submit wire-shape asserts incl. staged src path + task_type/track_name/global_caption/local prompt, 2-stem import+undo, lego 1-track import+undo, error paths verbatim), independently re-run by orchestrator (2026-07-10)
- [x] (iii-c-real) Real-sidecar stems gate — COMPLETE (2026-07-10, gate3 run 5: **10/10 with SFT authenticity log-proven** — every extract sub-job routed "Using second model: acestep-v15-xl-sft", zero silent substitution; stems are distinct real PCM separations (different md5s, 5.5 MB RIFF/WAVE 48 kHz each), 2-violet-track import + single undo, Phase B 164 s incl. slot-2 load; archived to ~/Library/Application Support/DAWPro/Generations for audition). Decisions landed: stems `model:nil` defaults to `acestep-v15-xl-sft` (client constant) and the client ENSURE-LOADS it — `GET /v1/model_inventory` is_loaded check → `POST /v1/init {model, slot:2}` (600 s configurable timeout) with per-instance ensured cache; run.sh exports `ACESTEP_CONFIG_PATH2=acestep-v15-xl-sft` (slot 2 constructed at startup); actionable `modelSlotUnavailable` error when an old sidecar lacks the env var. TWO more REAL-gate-only bugs fixed en route: `/v1/models` is SHADOWED upstream by an OpenAI-compat route (`{"object":"list","data":[]}`) — the internal `/v1/model_inventory` is the correct envelope (live-confirmed; it also proves only sft advertises extract/lego/complete in supported_task_types); and the slot-2 load briefly stalls the event loop (GIL) so submits can bounce with the misleading "installed but not running" — wire clients need transient-tolerant submit retry (third bite for the "loading" state polish item). PARTIAL-run history preserved below in git-less form: run 1 caught the mp3 audio_format omission (fixed same day). 1035/127, 83 tools
- [x] AIServices wired end-to-end with key management UI (Keychain) — security design: key VALUES never transit the control wire/MCP (no set-key command anywhere, by design — documented in the command + ARCHITECTURE); keys enter via env (precedence, shown LOCKED in UI) or the Settings panel → `KeychainKeyStore` (SecItem generic-password, service `com.dawpro.api-keys`, AfterFirstUnlock). `KeyStore.swift`: `AIProviderID` (anthropic/openai/suno; ACE-Step keyless by design), `APIKeyStoring` + `InMemoryKeyStore` (lock-based Sendable, no @unchecked), env-first `resolveKey` chain reporting source, `AIConfig.fromEnvironment(keyStore:)` additive (nil = prior behavior). Headless `DAWAppKit.SettingsModel` (draft entry, save/clear, session-only `•••• last4` mask — stored plaintext never read back, env rows locked, whitespace-paste validation) + glass Settings overlay (gear chip + ⌘,; neutral chrome, NO violet — not AI content). Wire: `ai.providerStatus` `{provider, configured, source}` only + `ai_provider_status` MCP tool (83→84); debug-tier `ui.showSettings`/`debug.settings*` off allCommands/MCP. 1062 tests/130 suites (+27/+3 incl. live headless Keychain round-trip + full-JSON no-leak scan + no-set-key-exists assertion), 0 warnings, npm clean; both captures orchestrator-reviewed (empty state; env-locked Anthropic + keychain-configured OpenAI w/ mask + red CLEAR); live wire round-trip re-run independently ALL PASS incl. raw-JSON key-material scan (2026-07-10)
- [x] Lyrics workshop: Anthropic/OpenAI-powered writing/editing panel tied to project key/tempo, feeding ACE-Step's bracketed-structure lyric format — **REAL-KEY GATE PASSED 2026-07-11 → COMPLETE** (Anthropic key landed in Keychain; orchestrator ran one real `ai.writeLyrics` on 17695 through the (m10-a)-fixed parser: bracketed [verse]/[chorus] synthwave lyrics, 773 chars, provider=anthropic, 5.3 s, 7/7 checks). Original stub-complete record: **STUB-COMPLETE 2026-07-10**: full vertical shipped and independently verified — additive `writeLyrics(LyricsWriteRequest)` on `LyricsGenerating` (default-bridged) + shared `LyricsPromptBuilder` (identical bracket-format + singability system prompt for both providers, project tempo/meter woven in from `store.transport` when omitted) + anthropic-first/openai-fallback `resolveLyricsWriter` over the Keychain/env chain with an actionable key-value-free no-key error naming Settings ⌘,; headless `DAWAppKit.LyricsWorkshopModel`; violet WRITE-WITH-AI disclosure in the Sketchpad (write/rewrite/refine/apply-to-lyrics); `ai.writeLyrics` command + `ai_write_lyrics` MCP tool (84→**85**). 1099 tests/135 suites (+37/+5), 0 warnings, npm clean; both-provider request shapes pinned against stub HTTP servers; 3 captures orchestrator-reviewed (filled composer / "via anthropic" draft w/ REWRITE·REFINE·APPLY / bracketed lyrics applied); live no-key wire round-trip ALL PASS (actionable error, zero key material, field-named validation). On key arrival: run one real `ai.writeLyrics`, eyeball the bracketed output, then check this box
- [x] (v-a) Repaint core — `ACEStepClient` repaint jobs against the verified upstream surface (`task_type:"repaint"`, `src_audio_path` staged into the shared-$TMPDIR allowlist like stems, `repainting_start`/`repainting_end` seconds, `repaint_mode` conservative/balanced/aggressive + `repaint_strength` 0–1 (balanced only), `repaint_wav_crossfade_sec`, seed both ways (`use_random_seed` explicit), ALWAYS `audio_format:"wav"`; repaint is supported by BOTH turbo and sft per TASK_TYPES_TURBO/BASE read from source 2026-07-10 — so NO sft ensure-load, single raw job id through the ordinary `generationStatus` path, no composite prefix); additive `SongGenerating.repaintAudio` (default throws, fakes unaffected) + `ai.repaintAudio` command (field-named validation incl. FileManager existence check on `sourcePath`; `latentCrossfadeFrames` deliberately Swift-API-only, command/MCP omit it → sidecar default 10) + `ai_repaint_audio` MCP tool (85→**86**); "retake" = re-run same window w/o `seed` (no upstream retake type — documented, no invented command). **COMPLETE 2026-07-10**: 1118 tests/137 suites (+19/+2), 0 warnings, npm clean; wire shape pinned vs stub sidecar (start/end/mode/strength/crossfade/seed/wav + no inventory/init calls); live app round-trip ALL PASS (4 field-named validation errors + actionable sidecar-unreachable on well-formed submit). Real generation deferred to (v-c) gate by design
- [x] (v-b-1) Clip vocal-fix flow, agent path — **COMPLETE 2026-07-10** per the settled design (`docs/research/design-v-b-clip-fix.md`, daw-architect): dry as-heard windowed bounce (region ± 10 s context clamped, plain-clip fades STRIPPED to match post-grouping lane playback; whole-clip bounce rejected — job time scales with length and outside-window returns are VAE reconstructions that must never play), absolute-timeline-beat wire addressing (`ai.fixClipRegion {trackId, clipId, startBeat, endBeat, …}`), take lands `ratio 1.0/offset 0/gain 0` violet "AI Fix N" at the window position, comp SPLICE of the fix region only via M5 take groups (one `performEdit("AI Fix Take")` — undo restores the plain clip/previous comp exactly), explicit submit → `ai.generationStatus` poll → `ai.importClipFix` with in-memory `pendingClipFixes` registry (cleared on project switch) + pure-move rebase / honest `clipFixStale` revalidation (group id = stable anchor; tempo change always stale); queued fixes on the same clip compose into one group. Landed: `DAWCore/ClipFix.swift` (+`ClipFixPlanner` pure window/splice), `ProjectStore+ClipFix.swift`, `GenerationImporting.submitRepaint` seam, 2 commands + `ai_fix_clip_region`/`ai_import_clip_fix` MCP tools (86→**88**). Verified independently: **1152 tests/140 suites** (+34/+3), 0 warnings, npm clean at 88, live app round-trip ALL PASS (7 checks: field-named validation, ghost-track translation, exact `clipFixJobNotFound` contract message). Real-sidecar boundary continuity deferred to (v-c) by design
- [x] (v-b-2) Clip vocal-fix flow, UI affordance — **COMPLETE 2026-07-10**: headless `DAWAppKit.ClipFixModel` (@MainActor @Observable; cards pending/running(progress)/succeededAwaitingImport/imported(laneName)/failed/stale + transient isStale failed-poll tolerance; injected submitter/status/importer closures; `clipFixStale`→terminal amber, `clipFixJobNotFound`→terminal red, other import errors→transient retryable — Sketchpad rules) + violet FIX WITH AI chip in the arrange header (shown for selected plain audio clips) opening a docked 340pt glass panel: composer (cyan SF Mono START/END beats prefilled from the clip, prompt/lyrics, STRENGTH SUBTLE/BALANCED/BOLD → conservative/balanced/aggressive, violet-glow FIX THIS REGION) + AI FIXES jobs strip (shimmer/READY+IMPORT/IMPORTED→lane badge/red failed w/ dismiss); panel persists across selection changes (composer collapses to hint, jobs keep polling via view-owned 1s task); debug-tier `ui.showClipFix`/`debug.clipFixSeed`/`debug.clipFixState` off allCommands/MCP; DESIGN-LANGUAGE entry added. **1171 tests/141 suites** (+19/+1), 0 warnings, MCP untouched (88); 3 captures orchestrator-reviewed (filled composer / running+ready+failed variety / imported badge)
- [x] (v-c) Real repaint gate — **COMPLETE 2026-07-10 (run by orchestrator, not delegated)**: full live loop against the real sidecar on the archived 30s vocal stem — project scaffold via wire → `ai.fixClipRegion` (beats 16–24 @120bpm, balanced/0.5, ±10s context → window beats 0–44, repaint 8–12s in-file; accepted FIRST attempt) → status polls (3 model-load GIL stalls absorbed by tolerance exactly as designed) → 8-step MLX turbo diffusion, 48kHz WAV → `ai.importClipFix` landed "AI Fix 1", comp splice EXACTLY [orig 0–16 | fix 16–24 | orig 24–60] → `render.bounce` audition. **Continuity analysis PASS**: context outside the repaint window returned at peak lag **0 samples, correlation 1.0000** both sides (sample-exact preservation — upstream splices source audio back outside the window, not VAE recon); fix region genuinely repainted (corr 0.16); both comp seams click-free (max delta 0.1×/1.28× global p999, threshold 3×). ~70s wall end-to-end incl. model load. 9 artifacts archived for audition: `~/Library/Application Support/DAWPro/Generations/gate-vc-repaint/` (dry window bounce, returned repaint, comped bounce, analysis.json, submission/status/import/snapshot JSONs)
- [x] (v-d, stretch) Auto-align generated vs recorded vocal takes (onset-based micro-alignment) — **COMPLETE 2026-07-10**: pure `DAWCore.TakeAligner` (1 ms grid search over ±searchWindow, greedy once-consumable onset matching, deterministic tie-breaks, MEDIAN refinement of matched deltas — recovers a planted 80 ms shift to 8e-14 ms through the REAL spectral-flux detector) + `autoAlignTake(trackID:groupID:laneID:searchWindowMs:apply:)` (reference = lane 0, overlap-windowed timeline mapping, onsets via the SAME `detectTransients` seam quantize uses — no duplicated DSP; `apply:false` dry-run preview; apply = one undoable "Align Take" via the take.move machinery) + `take.autoAlign` command (searchWindowMs 10–500 field-named validation) + `take_auto_align` MCP tool (88→**89**) + `ProjectError.alignmentInconclusive` / `alignmentWouldCrossTimelineStart` (headroom guard: apply that would push the lane before beat 0 THROWS with required-move/headroom/take.move advice — NO partial clamp, `applied:true` strictly means aligned; bug caught by the orchestrator's live closed-loop round-trip, fixed same cycle + 3 regression tests). **1202 tests/145 suites** (+31/+4), 0 warnings, npm clean; live round-trip **9/9 ALL PASS** (preview +80 ms ±3, preview purity, apply→residual <3 ms closed loop, lane-0/range/no-headroom errors, beat-0 preview purity, undo restore)
- [x] Copilot rail v1: chat panel driving the control API with session context — **COMPLETE 2026-07-11** (rail-a…rail-e all landed; real-key gate passed, see rail-e):
  - [x] (rail-a) Architecture design — **COMPLETE 2026-07-10** (daw-architect, orchestrator-reviewed): settled design archived at `docs/research/design-rail-a-copilot.md` (11 sections, all decisions closed). Headlines: hand-curated `CopilotToolCatalog` in DAWControl (36 commands, schemas transliterated from mcp-server — the repo's only JSON-schema source; wire names map "."→"_" with bijection test); in-process execution through the SAME `CommandRouter.handle` the ws server uses (injected dispatch closure, tests drive the real router); **`CopilotEngine` lives in DAWControl not AIServices** (AIServices can't see CommandRouter/JSONValue — cycle; DAWAppKit mirror model DROPPED, DAWApp observes the @Observable engine directly); provider seam speaks pre-encoded `Data` (`CopilotProviding`, anthropic-first/openai-fallback per resolveLyricsWriter, per-turn resolution so mid-session keys work, non-streaming v1); context rebuilt every ROUND, 8-char id prefixes + engine-side expansion map; safety = positive allow-list + test-asserted denylist {project.new/open/**save**, track.remove, ai.copilot*} + transport.record/track.setArm omitted (mic stays human) + 8-round/4k-result/8k-context/20-msg caps; wire = ai.copilotSend/State/Reset + 3 MCP tools (89→92 — design doc says 88→91, count slip, names authoritative); ~41 tests in 4 suites incl. FakeCopilotProvider against the real router. v1.1 flag: per-turn named undo grouping needs a ProjectStore primitive that doesn't exist yet
  - [x] (rail-b) Provider tool-use client — **COMPLETE 2026-07-10**: `Sources/AIServices/CopilotProvider.swift` per design §2 verbatim — `CopilotToolSpec`/`CopilotContentBlock`/`CopilotMessage`/`CopilotTurnRequest`/`CopilotReply(StopReason)`, `CopilotProviding` speaking pre-encoded `Data` (no DAWControl import), `AnthropicCopilotProvider` (POST /v1/messages, x-api-key + anthropic-version, tools/input_schema, tool_use/tool_result blocks w/ tool_use_id + is_error, stop_reason mapping) + `OpenAICopilotProvider` (chat/completions function-calling fallback) + `resolveCopilotProvider` (anthropic-first Keychain/env chain, actionable ⌘, no-key error). 3 DEVIATIONs accepted: nested Role Equatable (compile necessity), max_tokens forwarded to OpenAI, explicit public inits (rail-c cross-module construction). Suite A = 13 tests/3 suites on the reused StubTextAPIServer: both-provider request-shape pinning, text/single-tool_use/MULTI-tool_use/max_tokens/malformed parsing, resolve chain order, no-key error asserts zero key material. **1215 tests/148 suites** (+13/+3), 0 warnings, MCP untouched (89); catalog-from-DAWControl lands in rail-c per design (the schemas live app-side, not in this client)
  - [x] (rail-c) Copilot engine + wire — **COMPLETE 2026-07-10** (design amended per rail-a D4: engine lives in DAWControl, DAWApp observes it directly — no `DAWAppKit.CopilotModel` mirror): `CopilotCatalog.swift` (36-command `CopilotToolCatalog.v1` w/ JSONValue schemas, `neverInclude` denylist, "."↔"_" mapping) + `CopilotEngine.swift` (@MainActor @Observable; send/cancel/reset/waitForTurn/stateJSON; per-ROUND context rebuild w/ 8-char id prefixes + expansion; up to 8 tool rounds; command errors → isError tool_results; 4k/8k/20-msg caps) + 3 ai.copilot* commands (allCommands, weak copilotEngine on the router) + DAWProApp wiring + `ai_copilot_send`/`_state`/`_reset` MCP tools (89→**92**) + ARCHITECTURE prose + settled placement bullet. 4 DEVIATIONs accepted (incl. write-blind draft discarded & rewritten per design — the write-first method working as intended; track.setInstrument catalog schema simplified, noted in its description). 32 tests/3 suites: catalog exhaustiveness (9), FakeCopilotProvider vs REAL router (15), command wire (8). Verified independently: **1247 tests/151 suites** (+32/+3), 0 warnings, npm clean at 92, live app round-trip **7/7 ALL PASS** (field validation, actionable ⌘, no-key error with engine wired, §11 unknown-turnId contract, reset, zero key material)
  - [x] (rail-d) Rail UI — **COMPLETE 2026-07-10**: `CopilotRailView` + `CopilotTranscriptEntryView` (Sources/DAWApp/Copilot/) — right-docked 360 pt violet-edged glass rail mounted APP-LEVEL (outermost-right, full workspace height, Arrange AND Mix; deliberate deviation from nesting beside ClipFix — copilot is not selection-gated); always-visible violet COPILOT header chip (`bubble.left.and.text.bubble.right.fill`, distinct from Sketchpad/ClipFix icons); header = glowing identity dot + AI credit + reset ↺ + close; per-kind transcript rendering (right user bubble / violet assistant glass / violet toolCall chips w/ dotted command + params / green-ok·red-error toolResult chips / red failure strip w/ actionable message), WORKING shimmer row, auto-scroll, beginner first-use hint card w/ 3 example prompts, input + violet send / red cancel while running; `CopilotSummaryFormat` chip compactor in DAWAppKit (headless-testable, ClipStretch precedent); `CopilotEngine.seedForCapture` debug hook + debug-tier `ui.showCopilot`/`debug.copilotSeed`/`debug.copilotState` off allCommands/MCP; DESIGN-LANGUAGE "Copilot rail" entry. **1257 tests/153 suites** (+10/+2), 0 warnings, MCP untouched (92); 3 captures orchestrator-reviewed PASS (idle hint / full entry-kind variety incl. error tool_result + shimmer / failure strip with input re-enabled)
  - [x] (rail-e) Real-key gate — **PASSED 2026-07-11 (orchestrator-run, 12/12)**: one real two-turn conversation against the live Anthropic API (Keychain key) on staging 17695, driving actual project edits end-to-end. Turn 1 "Add an instrument track named Neon Bass, and set the project tempo to 100 BPM" → engine issued `track.add` + `transport.setTempo` (both toolResults ok) in 6.0 s; `project.overview` confirmed the track exists, tempo 100, exactly one track added. Turn 2 "Now mute that track" → the copilot **re-oriented itself with `project.snapshot`, resolved the pronoun from conversation history to the correct trackId, and issued `track.setMute`** (16.0 s); overview confirmed muted:true with zero collateral edits. Whole-session transcript spans both turns (12 entries), zero key material. One cosmetic model quirk recorded (not a wire defect): turn 1's closing text hallucinated a pre-existing duplicate "Neon Bass" track — the edits themselves were exact.
- [ ] (dormant) Suno cloud fallback behind SongGenerating — only if an official API materializes

## M7 — Full MCP control surface ✅
- [x] Every control command exposed as an MCP tool with rich schemas — **COMPLETE 2026-07-10**: coverage already held 89/89 (scoping inventory; the apparent gaps were naming-convention artifacts) — the deliverable was ENFORCEMENT: `mcp-server/test/audit-tools.test.ts` (node:test, zero new deps) spins the REAL server on `InMemoryTransport.createLinkedPair()` + SDK Client, calls tools/list, parses `Command.allCommands` from Commands.swift (depth-aware bracket scan, <80-commands format-drift guard), asserts bijection + richness: every tool titled + ≥40-char description, every inputSchema property recursively described, no capitals/dots in names. Exception tables documented in test + ARCHITECTURE: A = 6 generation tools that historically drop the `ai_` prefix (generate_song, extract_stems, lego_generate, generation_status, import_generation, import_generated_stems — live in agent configs, never rename); B = 3 direct-API tools with no command (generate_image, generate_lyrics, generate_song_suno). Wired as `npm test` (tsconfig.test.json → dist-test/, gitignored). Refactor: `src/index.ts` (3870→30 lines, stdio entry only) + new `src/server.ts` (all 92 registerTool, exported pre-connect — future tool edits + greps go THERE). Audit caught real gaps pre-fix: track_rename description under bar, automation_add_lane discriminator literals undescribed ×4. Verified independently: cold `npm test` 7/7, stdio initialize→tools/list on dist/index.js returns exactly 92, NEGATIVE test (planted short description → exactly the richness assertion fails 6/1, restore → 7/7), Swift suite untouched; /mcp-verify skill step 2 updated to run the automated audit
- [x] Agent-facing project snapshot: structured, token-efficient session state — **COMPLETE 2026-07-10**: `project.overview` / `project_overview` (90 commands, **93** MCP tools) — `ProjectOverview` Codable projection in DAWCore (`ProjectStore.overview()`, pure read-only, headless-tested) encoding transport/master/tracks with full actionable UUIDs and counts-not-lists everywhere unbounded (MIDI → noteCount never notes, automation → pointCount never points, NO file paths); optional fields omitted not null (output-when-bus, instrument-on-instrument-tracks resolved nil→default per house pattern, hasStretch/hasFades/gainDb, takeLaneCount/activeLane comp-derived agree-or-nil). Token efficiency is a TESTED contract: synthetic dense project (8 tracks/24 clips/64-note MIDI/60-pt lanes) must encode <8 KB and ≥5× under project.snapshot — measured **6,537 B vs 84,731 B (12.96×)**. Known v0 seam: sends report `preFader:false` constant (model is post-fader-only; field kept for shape stability, documented). Verified independently: 0 warnings, **1263/154** (+6/+1), npm audit 7/7 with bijection auto-adjusted (90+3=93 — the iter-79 enforcement carried this item's parity for free), live wire 8/8 (tempo/mute surfaced, noteCount-not-notes, track id round-trips actionable, no path leaks, 2.31× smaller even on a 3-track session)
- [x] Composition macros — split 2026-07-10, ALL THREE COMPLETE 2026-07-10 (each = store method + command + MCP tool + tests; command names follow the mechanical naming rule rather than the original underscore suggestions so the parity audit needs no new exceptions):
  - [x] (macro-a) `clip.humanize` / `clip_humanize` — **COMPLETE 2026-07-10**: `humanizeClipNotes(clipID:timingBeats:velocityRange:seed:)` in `ProjectStore+Humanize.swift` with a splitmix64 `SeededRandomNumberGenerator` (system RNG isn't seedable) — independent uniform onset jitter ±timingBeats (clamped strictly inside the clip, overhang onsets pulled back under the end) and velocity jitter ±velocityRange (clamped 1–127); lengths/ids/order preserved; comp members rejected with `clipInTakeGroup` (guard added after orchestrator premise-check bounced the agent's DEVIATION — store invariant at 10+ sites wins over the brief's incomplete error list); one `performEdit("Humanize")`, and per the app-wide performEdit convention a zero-jitter call journals NOTHING (agents: don't undo after a no-op). Wire: `clip.humanize` {clipId, timingBeats 0–0.25 default 0.02, velocityRange 0–64 default 8, seed <2^53} → clip fields + **`seedUsed` sibling key** (drawn seeds land in [0,2^53) so they survive the JSON number wire — replay to reproduce, omit to re-roll); MCP `clip_humanize` (**94** tools). 15 new tests (9+1 DAWCore incl. comp-member regression, 5 wire incl. same-seed-through-the-wire). Verified independently: 0 warnings, **1278/156** (+15/+2), npm audit 7/7 at 91+3=94, live round-trip **10/10** (seed 42 echo + reproducibility, both clamp rails, bit-identical no-op, drawn-seed integer, field-named rejections)
  - [x] (macro-b) `mixer.applyPreset` / `mixer_apply_preset` — **COMPLETE 2026-07-10**: `applyMixerPreset(trackID:presetName:)` in `Sources/DAWCore/MixerPresets.swift` over a static, versioned, code-defined catalog (`MixerPresetCatalog.v1`, the `CopilotToolCatalog` precedent). Six presets — `drum-bus-glue`, `vocal-presence`, `bass-tight`, `master-glue`, `warm-keys`, `clean-boost` — each a `MixerPreset {name (kebab id), displayName, summary, chain: [EffectDescriptor]}` mapping a musical intent onto the REAL built-in param surfaces (`EffectParamSpec`) using only the FX pack's EQ/compressor/limiter/gain. The chain REPLACES the strip's current insert chain wholesale in ONE `performEdit("Apply Preset '<DisplayName>'")` — a single undo restores the exact prior chain (ids + order) — volume/pan/sends untouched (tone, not balance); works on audio, instrument, AND bus tracks. Each apply mints FRESH effect ids (`freshChain()`) so two strips holding the same preset address distinct effects. DEVIATION (noted in catalog + report): the built-in EQ has no dedicated high-pass, so every "rumble/sub trim" intent is a low-shelf with negative gain (closest available shape — no new effect type added). Wire: `mixer.applyPreset` {trackId, preset (one of six)} → shared fx-mutation shape `{trackId, effects:[{id, kind, isBypassed, params, latencySamples}]}` (resolved new chain, in order); unknown preset → `mixerPresetNotFound` LISTING every valid name (the `unknownEffectParam` precedent), `trackNotFound` via existing mapping. MCP `mixer_apply_preset` (`preset` zod enum, each described; **95** tools). 18 new tests (12 DAWCore incl. catalog integrity via APPLYING each preset + all-three-track-kinds + undo-exact-prior-chain, 6 wire incl. field-named validation + name-listing + happy-path chain-in-order). Verified: 0 warnings, **1296/158** (+18/+2), npm audit 7/7 at 92+3=95; orchestrator re-ran all three suites (identical results) + live wire **8/8** (vocal-presence replaced a pre-existing gain effect with eq→comp at exact catalog params, undo restored the prior single-gain chain, clean-boost gainLinear matched 10^(3/20) to 1e-9, unknown preset listed all six names, master-glue landed comp→limiter on a bus).
  - [x] (macro-c) `macro.songSkeleton` / `macro_song_skeleton` — **COMPLETE 2026-07-10**: `SongSkeletonCatalog.v1` + `applySongSkeleton(genre:tempoBPM:sections:)` in `Sources/DAWCore/SongSkeleton.swift` — five genres (pop 120 / house 124 / hip-hop 90 / rock 140 / ballad 72), each a roster of named tracks with `MixerPresetCatalog.v1` presets pre-applied (bass-tight/warm-keys/vocal-presence; catalog-integrity test validates every cross-reference — macro-b composed into macro-c); sections land as contiguous named EMPTY MIDI clips on an "Arrangement" instrument guide track (bars×4, 4/4), loop region 0→total; ADDITIVE to the current project. **Single-undo-step finding**: `performEdit` journals one entry PER CALL (UndoJournal coalesces only same non-nil key within 800 ms), so nesting store methods would fragment undo — everything folds into ONE performEdit body via no-edit helpers (`applyTempoChange` precedent + new `applyLoopRegion`), proven at both test layers. Params: genre (unknown → error listing all five), tempoBPM? 20–400 (default genre's), sections? 1–16 × {name ≤40, bars int 1–64}; response: {genre, tempoBPM, tracks[{id,name}] (Arrangement last), sectionClips[{name,startBeat,lengthBeats}], loopStart/End, arrangementTrackId}. MCP `macro_song_skeleton` (genre zod enum; **96** tools). 22 new tests (15 DAWCore, 7 wire). Verified independently: 0 warnings, **1318/160** (+22/+2), npm audit 7/7 at 93+3=96, live wire **10/10** (additive on non-empty project, bass-tight chain landed, 8 empty guide clips, Outro at 192/loop 208, ONE undo → exactly the pre-existing track, custom sections, both error contracts)
- [x] Demo: agent composes/arranges/mixes a track end-to-end over MCP, zero clicks — **COMPLETE 2026-07-10**: a delegated Claude agent, restricted to the `mcp__daw-pro__*` tool surface, composed/arranged/mixed a 24-bar synthwave piece (104 BPM, Am–F–C–G, 464 notes, 4 polySynth tracks + "Space" reverb bus; real sections — Chords enter bar 5, Lead bars 9–20 only, outro fade via a 4-point volume lane), proved playback with live meters, bounced to −14 LUFS target (ceiling-clamped, sample peak EXACTLY −1.000 dBFS) and saved the bundle — ≈52 MCP calls, zero clicks, zero agent-written files; artifacts + DEMO-NOTES archived in `~/Library/Application Support/DAWPro/Generations/demo-m7-agent-e2e/`. Orchestrator verification: project.json matches the brief on every axis; independent wire loudness re-measure matched the agent's numbers exactly (−19.19 LUFS / −6.04 dBTP); independent play+snapshot showed intro meters with Lead correctly silent. Demo doubled as a live MCP audit → 2 real current-code mcp-server bugs queued for the integration-suite item: (bug-1) void-result tools serialize `JSON.stringify(undefined)` → invalid content item → client `-32602` though the command executed (`server.ts textResult`); (bug-2) bridge `REQUEST_TIMEOUT_MS = 5000` eats render-family replies on full-length renders (~25 s wall) — file lands, report lost; plus: project_snapshot inlines every note (103 KB at 464 notes) — already solved by project.overview, but the demo ran against a STALE MCP server process predating the macros/overview (agent composed the long way; strengthens the demo). Ops learning recorded in memory: subagent output files no longer stream tool rounds — two healthy agents were falsely killed as stalls before the artifact-side-effect monitoring doctrine replaced stat-polling; the second was resumed from transcript and finished
- [x] MCP integration test suite (drives a headless app instance) — **COMPLETE 2026-07-10, closes M7**: `mcp-server/test/integration.test.ts` (12 tests, joined into `npm test` after the audit) drives the REAL wire — spawns the actual `.build/debug/DAWApp` binary (`DAWPRO_APP_BINARY` override; missing binary → whole file SKIPS with an actionable message, never a silent pass) on loopback port 17690 plus a real SDK Client over `StdioClientTransport` launching `node dist/index.js`, so every call genuinely crosses client → stdio → mcp-server → control ws → app. Coverage: tool-count parity, project/track/clip round trip, ALL THREE composition macros over real MCP for the first time (skeleton scaffold + bass-tight chain + seeded humanize determinism — with an `edit_undo` between same-seed calls, agent-caught semantic requirement since humanize composes on current notes), skeleton one-`edit_undo` revert, `project_overview` <8 KB, error surfaces verbatim, `render_bounce` re-measured output. OPENED by fixing the demo's two live mcp-server bugs with regression pins: (bug-1) `textResult` void-result contract — `undefined` now reads literal `"ok"` instead of `JSON.stringify(undefined)` → invalid content item → client `-32602` despite the command executing; (bug-2) `DawBridge` per-command timeouts — `LONG_RUNNING_COMMANDS` {render.bounce/mixdown/stems/measureLoudness} get 180 s vs the flat 5 s that discarded ~25 s render replies (regression proves a 6.2 s measure survives). Teardown SIGTERM→SIGKILL in try/finally — `pgrep DAWApp` empty after every run incl. failures/skip path. Verified independently: **npm test 19/19 in ~11.4 s** (audit 7/7 intact), Swift baseline untouched **1318/160**, both fix diffs surgical, ARCHITECTURE "Integration suite" paragraph accurate. Suite landed 19/19 on its FIRST run — the agent spent its time reading real schemas/handlers instead of debugging

## M8 — Design & simplicity polish
- [ ] Full glass-cockpit pass per DESIGN-LANGUAGE.md — split 2026-07-10 (the GPT-Image asset half is key-blocked; the compliance half is viable now):
  - [x] (glass-a) App-wide design-compliance audit + fixes — **COMPLETE 2026-07-10**: full sweep of all 10 shipped surfaces (code sweep + 11 headless captures on port 17695, each viewed) against DESIGN-LANGUAGE.md; findings archived at `docs/research/design-audit-glass-a.md` (per-surface verdicts w/ file:line evidence). The codebase was already highly compliant — essentially all color routes through `DAWTheme`, every numeric readout SF Mono, glow state-only, violet strictly AI. TWO hard rule breaks found + fixed: sidebar Mute chip amber→red (`TrackListView.swift` — Rule 3 + "Mute = red"; amber had made M and R chips indistinguishable; mixer already used red) and piano-roll keyboard raw hex → new `DAWTheme.keyBlack/keyWhite` tokens (zero visual change, kills the last raw hex in the view layer). Verified independently: build 0 warnings, suite **1318/160** held exactly, before/after captures reviewed at pixel level (arrange M-chip red; pianoroll identical), no strays. Three restyle-sized FOLLOW-UPs deferred per the no-half-fix rule → (glass-c)
  - [x] (glass-c) Compliance FOLLOW-UPs from the audit — **COMPLETE 2026-07-10** (execution addendum + computed tables appended to design-audit-glass-a.md): FU-1 the three create "+" affordances (add-track, add-insert, add-send) dropped cyan for neutral `textPrimary` per Rule 3 — "an accent is earned by state, not by inviting a click" now codified in DESIGN-LANGUAGE.md (Sketchpad ± stepper correctly exempted as a numeric-readout control); FU-2 measured WCAG pass (alpha-composited over actual surfaces, math in scratchpad/contrast.py) — plain `textDim` measured 5.51–6.30:1 and left alone; two new tokens `textSecondary` #9FA9BC (real labels ≥4.5, "No inserts/No sends" 3.39→6.71) + `textFaint` #767E90 (placeholder floor ≥3.0, worst case 2.84→4.18) replaced 9 ad-hoc `.opacity()` sites; 4-tier hierarchy (primary 14.4–16.4 › secondary 7.2–8.2 › dim 5.5–6.3 › faint 4.2–4.8) codified in the doc; FU-3 `DAWTheme.gridEmphasis` = white@0.14 replaced the 3 literals (pianoroll −0.02, keyboard +0.02 — grid now uniform with the timeline). 13 files; verified independently: build 0 warnings, suite **1318/160** held, before/after captures reviewed (mixer "+" neutral + labels legible; before-binary built by reverse-applying the 10 call-site edits since the uncommitted tree made git stash unusable)
  - [ ] (glass-b) GPT Image-assisted asset set (backgrounds/textures/iconography where hand-drawn Canvas isn't enough) — BLOCKED on OPENAI_API_KEY (see ## Blocked)
- [x] Simple ↔ Pro progressive disclosure on every panel — **COMPLETE 2026-07-10** (split sp-a..sp-d, all landed same day): density coverage is UNIFORM — four surfaces carry live `SimpleProToggle` chips (piano roll, mixer, arrange, transport), nine surfaces documented MODES-COINCIDE (already the beginner set; must never grow a do-nothing toggle — DESIGN-LANGUAGE "Panels" + inventory §"Density adoption status" codify it); per-panel sticky UserDefaults prefs proven independent on disk (4 keys, mixed values):
  - [x] (sp-a) Inventory + shared mechanism — **COMPLETE 2026-07-10**: 13-surface inventory at `docs/research/simple-pro-inventory.md` (file:line evidence) — 1 HAS-MODES (piano roll), 3 NEEDS-MODES (mixer channel strip; arrange timeline+clip chrome+snap; transport, minor), 9 MODES-COINCIDE with rationale (master strip, track rows, automation, takes, clip-fix, sketchpad, lyrics, copilot, settings — already the beginner set). Mechanism shipped: `PanelDensity` + `PanelDensityStore` (`Sources/DAWAppKit/PanelDensityModel.swift` — @Observable, per-panel, injected `PanelDensityBacking` seam; InMemory + UserDefaults `panelDensity.<panelID>` backings; 9 tests), shared `SimpleProToggle` chip extracted verbatim from the piano roll's modeToggle, piano roll refit onto store+chip under ID `"pianoRoll"` (sole behavior change: mode sticky across reopen/relaunch — by design), `debug.panelDensity {panel, mode}` staging command (debug tier: off allCommands, no MCP tool — the ui.show*/debug.*Seed precedent; contract tested hermetically in PanelDensityStoreTests since app-target debug handlers aren't SwiftPM-testable), DESIGN-LANGUAGE "Panels" codified (per-panel app-sticky preference, never project data). Verified independently: build 0 warnings, suite **1327/161** (from 1318/160), both mode captures pixel-reviewed (Simple↔Pro delta exactly {snap picker, velocity lane}; chip chrome verbatim), my own wire round-trip (both happy paths, both error messages, allCommands exclusion) + persistence proof (key deleted → wire set → app killed → `panelDensity.pianoRoll = pro` on disk)
  - [x] (sp-b) Mixer console Simple mode — **COMPLETE 2026-07-10**: the console is ONE panel (`MixerView.panelID = "mixer"`) on the sp-a store; the three Pro sections (inserts `MixerStripView.swift:90`, sends `:141`, output `:210`) hide WHOLE behind `if isPro` (`:39-44` — bus strips gate the same, keep dropping sends/output/Arm per kind); Simple strip = name/kind badge + pan + fader/meter/dB + M-S-A with the freed space flowing into a LONGER fader throw (`faderAndMeter` maxHeight, deliberate beginner win); shared `SimpleProToggle` in the Mix toolbar row top-right (the SNAP-chip position, `ContentView.swift` mixToolbar); master strip never reads the store (MODES-COINCIDE). Pure view-conditional — no new headless logic, no new tests needed, no new commands (sp-a's `debug.panelDensity` staged everything). Verified independently: build 0 warnings, suite **1327/161 held**, three captures pixel-reviewed (Simple hides exactly the three sections; Pro reveals real content — EQ+Compressor, −6.0 send mini-fader, Master routing; master pixel-identical; piano-roll regression clean) and the regression shot doubles as a live per-panel-independence proof: `panelDensity.mixer = pro` + `panelDensity.pianoRoll = simple` simultaneously in UserDefaults
  - [x] (sp-c) Arrange workspace Simple mode — **COMPLETE 2026-07-10**: panel ID `"arrange"`; Simple = see clips, drag the BODY to move on a Bar-locked grid, play; Pro = today's full edit layer. Chrome gated whole (`TimelineLanesView.swift` — fade shading+grips via `decorations` :702, ⌥-stretch cue :593, gain chip :473; snap picker Pro-only `ContentView.swift:213` beside the shared chip in SNAP's slot); gestures gated at the entry points, no dead hit-zones (`including: pro ? .all : .subviews` on stretch/split/fade-toggle :544-547; `beginDrag` collapses every zone to `.body` in Simple :844 so an edge press MOVES; ⌥-flags monitor never installed in Simple :553; context menu take entries both modes, clip edits Pro-only :972 — empty builder in Simple = no menu). Snap lock = tested DAWAppKit helper `ClipSnap.effective(density:picked:)` (never mutates the picker; Pro restores the choice); TakeLanesView keeps raw snap (take lanes density-unaffected). Read-only STATUS chrome (stretch ratio badge, error dot, shimmer, take badge) deliberately stays in both modes — a Simple user sees state, can't initiate the edit. Verified independently: build 0 warnings, suite **1328/161** (+1 effectiveSnapDensityLock), three captures pixel-reviewed (Simple: plain block, no picker; Pro: fade wedges + −3.5 dB chip + SNAP picker; mixer regression clean), gesture gating code-reviewed site-by-site, and three independent sticky keys on disk (arrange=pro, mixer=pro, pianoRoll=simple)
  - [x] (sp-d) Transport split + modes-coincide close-out — **COMPLETE 2026-07-10**: panel ID `"transport"`; Simple hides exactly PUNCH (`TransportBar.swift:117-119`, LOOP+CLICK close up adjacent) and the test-tone button (`:57-59`); everything else stays (RTZ/play/record, LOOP, CLICK, Position/Time, tempo, master); shared chip far right past the master (`:66-70`). Labeled deviation, verified sound: `transportButtons` pinned to 340 pt leading width so the Position/Time/Tempo readouts sit at pixel-identical x in both modes (the visual anchor) while the chip row still closes up with a pure `if isPro` — the agent caught PUNCH truncation at 312 pt in its own capture review and re-captured at 340. Docs close-out: inventory §"Density adoption status" (4 LIVE CHIP / 9 COINCIDENT-EXEMPT) + DESIGN-LANGUAGE uniform-coverage line. Verified independently: build 0 warnings, suite **1328/161 held**, capture pair pixel-reviewed (PUNCH/fork gone in Simple, restored untruncated in Pro, readouts anchored), 4 independent sticky keys on disk (transport=pro, arrange=pro, mixer=pro, pianoRoll=simple)
- [x] "Explain this" AI affordance on hover for every control — **COMPLETE 2026-07-11** (ex-a mechanism + ex-b app-wide coverage + ex-c live hand-off gate all landed):
  - [x] (ex-a) Mechanism + catalog + reference adoption — **COMPLETE 2026-07-10**: violet "?" EXPLAIN header chip (`ContentView.swift:435`, SKETCHPAD-chip idiom) toggles sticky explain MODE (Esc exits; the Esc handler is mounted ONLY while active, `:445`); `.explainable(_:)` / `.explainable(_:when:)` reports control frames via `ExplainFramePreferenceKey` into `ExplainCoordinator` (`Components/Explain.swift:42`, 220 ms dwell-in / 260 ms grace-out), and a click-through `ExplainOverlay` (`:270` — explain is an overlay, NOT a lockout; empty area has no hit-testable content) anchors the violet-edged glass `ExplainCard` (~300 pt, flips above its anchor near the bottom edge). Copy = headless 21-entry `ExplainCatalog` in DAWAppKit (`ExplainModel.swift`: 12 transport + 9 mixer-strip) with TESTED style rules (title ≤24 chars; body 40–280 chars, sentence-final punctuation, no unglossed jargon) + registered-ID completeness. "Ask the Copilot →" closes explain mode and prefills the rail via `AppModel.copilotDraft` → `CopilotRailView.draftPrefill` (labeled deviation: the engine has no draft API, so the hand-off routes through AppModel — DRAFT only, never auto-sent, engine turn loop untouched, works key-or-no-key). Second labeled deviation: mixer adoption tags the FIRST strip only (`explainReference` flag) — every strip tagging identical IDs would collide frames; ex-b generalizes. Staging: `debug.explainMode {on, focus?}` app-level debug-tier handler (`DAWProApp.swift:451`), off allCommands/MCP (grep-verified: no explain surface in DAWControl or mcp-server). DESIGN-LANGUAGE "Explain this" bullet incl. the every-new-control-ships-a-catalog-entry rule. Verified independently: build clean, suite **1338/162** (+10 tests +1 suite), four captures pixel-reviewed (chip rest/lit, transport card, mixer card — violet ONLY on the AI affordances per Rule 3; known transient: a tall mixer card can overlap its own strip's upper controls, ex-b may refine anchoring), 6/6 live wire checks on 17695 (on/focus/off round-trips + unknown-focus error path), hand-off code-reviewed draft-only end-to-end
  - [x] (ex-b) Coverage rollout — **COMPLETE 2026-07-10**: catalog **21 → 43** entries (`ExplainModel.swift`, MARK'd per surface) and `.explainable` now spans the app: mixer complete (EVERY channel/bus strip + `mixerMaster`; `MixerView.swift:34` tags all strips), piano roll (`pianoRollGrid/Velocity/Snap`), arrange (`arrangeSnap`, `arrangeAddTrack`, `trackRowIdentity` + M/S/A reusing the mixer IDs on track rows, `clipBlock`), AI panels (12 IDs across Sketchpad incl. Lyrics workshop, Copilot rail, FIX WITH AI), settings (`settingsGear`, `settingsApiKey` — copy explains Keychain storage, never surfaces key values). PER-INSTANCE FIX (the ex-a limitation, retired): hover reports the hovered instance's OWN frame — `ExplainCoordinator.controlHover(_:frame:hovering:)` re-anchors on same-ID/different-instance (`Explain.swift:89-97`), `presentedFrame` anchors the card, and the preference dict (now `[ExplainID: [CGRect]]`, append-merged) is capture-staging only; `explainReference` deleted. Labeled deviations, all verified sound: (1) `transportDensity` → shared `panelDensity` ID reused by all four SIMPLE/PRO chips (decision-3 one-entry rule); (2) `debug.explainMode` gained an optional `instance` selector (pre-authorized; debug tier, off allCommands/MCP — grep re-verified) so the wire can stage a non-first-strip card; (3) Canvas-internal clip/note-grid affordances get one honest-scope entry each (`clipBlock`, `pianoRollGrid`) instead of fake per-grip tags; (4) ContentView explain scope hoisted to wrap the Settings modal so its rows are explainable. Tests: completeness stays exhaustive over all 43 + a `catalogHasGrownAppWide` count-floor (≥43) pins against silent shrink. Verified independently: build clean, suite **1339/162** (grew from 1338), five captures pixel-reviewed (per-instance PROOF: "Volume Fader" card centered on the 3rd strip; "Track" card on the 2nd row; piano-roll SNAP, Sketchpad GENERATE, and an API-Key card floating over the Settings modal with only "Paste key…"/NOT SET visible — Rule 3 clean everywhere), 11/11 live wire checks on 17695 (new IDs incl. `panelDensity`, `instance: 2` echo, renamed `transportDensity` now errors, off clears focus+instance)
  - [x] (ex-c) Copilot handoff live gate — **PASSED 2026-07-11 (orchestrator-run, 7/7)**: the EXACT hand-off draft `ContentView.askCopilotAboutControl` composes (":508 — Explain Punch Recording — what should I do with it here?") sent through the rail's engine against the live Anthropic API (Keychain key, staging 17695, explain card first staged on `transportPunch` via debug.explainMode). Reply (10 s, 1515 chars) coherently explained punch-in/punch-out re-recording, offered the three legitimate prep commands (setLoop/setMetronome/seek), and correctly told the user that arming/recording stays in the app UI (record/arm are excluded from the copilot catalog BY DESIGN — the model articulated its own guardrail). **Zero tool calls, zero mutating commands, project.overview structurally identical before/after, zero key material.** Bonus systemic proof: the reply referenced the live project's actual tracks with no tool calls — the rail-a per-round context rebuild delivering real project state. Gate-script lesson recorded: wire JSON key order is not stable between calls (Swift dictionary encoding) — state-identity checks must compare canonicalized structure, not bytes.
- [x] Session vibe meter (signature visualization) — **COMPLETE 2026-07-10**, both sub-items landed (VISION.md:16 realized: the whole mix as a single glowing instrument, live in the transport bar in both densities):
  - [x] (vm-a) Master-mix analysis engine + wire — **COMPLETE 2026-07-10**: `MasterMixAnalyzer` (`Sources/DAWEngine/Analysis/`, the cycle's first write) — 2048-sample Hann frames @ 1024 hop, vDSP packed real FFT, everything preallocated at init (`init` :143-214), zero allocation/locks in the process path; 24 geometric bands 40 Hz→16 kHz (band value = mean bin power dB, floor −80 exact), short-term RMS `levelDB` (asymmetric one-pole), `peakDB` held w/ −20 dB/s release + snap-to-floor, `centroidHz` brightness, `flux` = normalized half-wave-rectified spectral flux 0–1 (instant attack, 0.75×/frame release, silence-gated). Analysis rides INSIDE the existing master meter tap (labeled deviation, correct: one tap per bus is an AVFoundation limit; meter computation byte-identical; `@Sendable` load-bearing annotation kept) — post-master-fader, so what you hear is what's analyzed; snapshot VALUE rides the same `Task { @MainActor }` hop as `MeterFrame` (~46 Hz). `MasterAnalysisSnapshot` in DAWCore beside `MeterFrame` (labeled deviation, correct: the protocol surface is DAWCore); NaN-poisoned frames skipped whole, every published field finite by contract, stopped sessions DECAY to exact floors. Wire: `mixer.masterAnalysis` (93→**94** commands) + `mixer_master_analysis` MCP tool (96→**97**; integration count pin bumped), ARCHITECTURE.md:57 deep-dive row. Verified independently: build 0 warnings, suite **1355/164** (+16 tests +2 suites: 13 DSP incl. 1 kHz-sine centroid/band-12/RMS pins, chunk-size invariance, stereo parity, poisoned-input; 3 command-level), npm 19/19 at 94/97, live wire 9/9 (exact floors stopped → level −20.7/peak −11.8/centroid tracking an arpeggio 598→1623 Hz between polls → exact floors again after stop + peak-release decay)
  - [x] (vm-b) The glowing instrument — **COMPLETE 2026-07-10**: a glowing ember ORB (`Sources/DAWApp/Components/VibeMeterView.swift`, ~74×44 pt) whose SILHOUETTE is the spectrum — the 24 vm-a bands become mirrored radial geometry (`silhouettePoints` :130, bass bulges the bottom, treble the top, midpoint-quad smoothed :167), centroid → hue along a warm-amber↔cyan log-frequency ramp (40 Hz→16 kHz), level → core brightness + glow radius, flux → shimmer rate; `TimelineView(.animation)` + Canvas at 60 fps, paused when the window is inactive. NO VIOLET by construction: the ramp's green channel stays high at both poles and a dedicated test sweeps the full ramp pinning the invariant (Rule 3). Headless `VibeMeterModel` (DAWAppKit) owns every mapping curve + asymmetric attack/release smoothing (0.05 s/0.32 s — the visual breathes, never jitters); 12 tests (+1 suite): floors → dormant EXACT, 1 kHz → mid hue, full-scale → brightness 1, monotonicity ×3, convergence, attack<release, short-band padding, non-positive-dt no-op, no-violet sweep. Placement: TransportBar left of the master cluster (`TransportBar.swift:66`), BOTH densities (read-only status chrome per the sp-* doctrine), 340 pt readout anchor untouched. Staging: `debug.vibeSeed` app-level debug-tier handler (`DAWProApp.swift:369/:490` — `{bands?[24], levelDB?, peakDB?, centroidHz?, flux?}` with mid-mix defaults, `{clear:true}` drops the override; off allCommands/MCP, engine untouched — view prefers `AppModel.vibeSeed` over the live `store.masterAnalysis()` poll). Explain catalog 43→**44** (`vibeMeter`, ExplainModel.swift:35/:157) + `.explainable` tag; DESIGN-LANGUAGE "Session vibe meter" bullet. Verified independently: build 0 warnings, suite **1367/165** (from 1355/164), six captures pixel-reviewed (dormant dim ember vs bass-heavy warm/bottom-bulged vs bright cyan/top-bulged vs live-playback green-teal mid-spectrum — four unmistakably distinct states, zero violet, PUNCH untruncated; explain card correct with violet only on AI chrome), 4/4 live wire checks on 17695 (explicit seed echo, `{}` defaults, 3-band seed → exact error, clear), and isolation grep/find: `vibeSeed` absent from DAWControl + mcp-server, no engine/control/MCP edits (npm counts hold 19/19 at 94/97)
- [x] Onboarding: first-song-in-10-minutes guided flow — **COMPLETE 2026-07-10**, all three sub-items landed and the live gate passed on the REAL generation path (VISION success criterion exercised end-to-end: fresh profile → auto-offered tour → AI-generated song → played → shaped → mixed → bounced to disk → completed, persisting across relaunch):
  - [x] (ob-a) Flow design + headless tour model — **COMPLETE 2026-07-10**: design doc at `docs/research/design-onboarding.md` (the ob-b contract) — the 7-step script (welcome → generate → listen → shape → mix → export → done) with per-step rationale, state-machine table, and a grounded file:line **signal wiring map** for all five signals (generation import + skeleton fallback, transport play, performEdit sites, mixer fader/pan/preset, render bounce/mixdown; orchestrator spot-checked three sites — exact). Load-bearing doc rule for ob-b: emit `editPerformed`/`mixerAdjusted` from their SPECIFIC UI sites, never one global journal observer, or the shape/mix steps collapse. Honest gaps flagged: no export UI and no template button exist today (net-new ob-b work, render/skeleton are control-plane-only). Model: `Sources/DAWAppKit/OnboardingModel.swift` — `OnboardingSignal` (5 cases) + `OnboardingStep` (7, CaseIterable order = tour order; both String-raw for the ob-b staging command), `OnboardingCatalog` (copy + anchor + expected signal per step; one source of truth), `@MainActor @Observable OnboardingModel` with `begin/advance/skipStep/signal/dismissTour/reset`: signal advances ONLY the matching active step (wrong/duplicate/inactive/post-dismissal = no-ops), `advance()` is the manual escape so a missed signal never traps anyone, `skipStep()` no-ops on welcome/done, terminals (`completed`/`dismissed`) never re-offer, `reset()` = the Settings replay seam. Persistence: injected `OnboardingStateBacking` (InMemory + UserDefaults `onboarding.state`, PanelDensity idiom), compact `active:<i>` form with out-of-range/malformed → `.inactive` fallback. Anchors reuse registered ExplainIDs (tested); labeled deviation, sound: `done` anchors `.aiCopilot` (Explain is a mode, not an ExplainID), `export` anchor nil until ob-b builds the control. ZERO store coupling (imports Foundation+Observation only) and zero app/engine/control/mcp edits (find/grep-verified). 34 tests +1 suite; verified independently: build 0 warnings, suite **1401/166** (from 1367/165); orchestrator caught + fixed one copy defect the agent missed (listen step said the vibe meter is "near the top" — TransportBar is the BOTTOM bar per ContentView.swift:69; suite re-run green)
  - [x] (ob-b) The guided UI — **COMPLETE 2026-07-10**: `OnboardingTourView.swift` (Sources/DAWApp/Onboarding/) — neutral-glass cards (GUIDED TOUR header, 7-dot progress + "N / 7" mono readout, title/body/CTA, "Skip step" on task steps only, quiet "Skip tour"), **NON-VIOLET by design** (Rule 3: cyan house-active accent only — even the done card that names Explain/Copilot); welcome/done centered, the five task steps coach-mark-anchor beside their catalog ExplainID with a glowing cyan ring around the control (never covered; flips above near the bottom edge; floats center when the anchor is off-screen), frames flow via the pre-authorized Explain.swift `shouldReportFrames` extension (:155/:163 — explain staging OR tour active). **Signal wiring = the observing adapter** (`OnboardingSignalAdapter.swift`, re-arming withObservationTracking): content 0→>0 → projectGainedContent, isPlaying false→true → playbackStarted, NEW `ProjectStore.lastEditEvent {seq,label,key}` (set in performEdit exactly when a journal entry records, seq ticks through coalescing; ProjectStore.swift:12/:88/:2000) → tested `OnboardingEditClassifier` (key-preferred taxonomy: mixer.master/track.volume:/track.pan:/Apply Preset → mixerAdjusted, all else → editPerformed — a fader move NEVER reads as editPerformed), NEW `renderCompletedCount` (both renderBounce :103 + renderMixdown paths) → renderCompleted; supersedes the ob-a per-site emission plan (addendum appended to design-onboarding.md) and gives every signal WIRE PARITY. Plumbing: AppModel.onboarding w/ UserDefaults backing (DAWProApp.swift:166), adapter :367, first-launch offer via ContentView.onAppear → offerTourIfEligible (:126/:375), Settings "Replay tour" row (SettingsView.swift:113 → replayTour :381), per-step CTA dispatch (ContentView.performTourPrimary :503). NET-NEW CONTROLS: transport EXPORT button (TransportBar.swift:281, both densities, NSSavePanel → renderBounce(toPath:), completion via renderCompletedCount — one path) + Sketchpad "USE A TEMPLATE" (SketchpadView.swift:240, neutral chrome below GENERATE, applySongSkeleton "pop") — ExplainIDs transportExport/sketchpadTemplate, catalog 44→**46**, export step anchor nil→.transportExport (the one allowed OnboardingModel change). `debug.onboardingState {set?, signal?}` app-level debug tier (DAWProApp.swift:595, off allCommands/MCP; set parses OnboardingState(persisted:), signal injects through model.signal). Two labeled deviations, both sound (done card centered via signal!=nil anchoring gate — brief supersedes catalog anchor; template button below not beside GENERATE). +15 tests +2 suites (10 seam + 5 classifier incl. real-store drift guard). One infrastructure stall (600 s stream watchdog) recovered via resume mid-cycle. Verified independently: build 0 warnings, suite **1416/168** (from 1401/166), six captures pixel-reviewed (welcome/done centered non-violet, listen ring on Play, mix ring on the Bass fader, EXPORT w/ PUNCH untruncated + readouts pinned, template subordinate), engine/control/mcp untouched + staging off the public wire (grep/find), and a 16/16 live wire walk of the WHOLE TOUR via real actions only — skeleton→active:2, play→3, tempo→4 (editPerformed), fader→5 (mixerAdjusted, anti-collapse proven live), 1 s bounce→6 (renderCompleted) — plus exact error messages, signal-on-done no-op, profile left dismissed
  - [x] (ob-c) Live first-song gate — **PASSED 2026-07-10 (orchestrator-run, MODE=generation — the full-value path, no fallback needed)**: fresh profile staged by deleting ONLY the `onboarding.state` key (density prefs preserved); on launch the AUTO-OFFER fired with zero wire intervention (first poll read `active:0`). The walk, real actions only for all five task steps: ACE-Step sidecar cold-booted to healthy over the wire (`ai.sidecarStart`, installedNotRunning → healthy), REAL 30 s generation ("a warm lo-fi beat with soft piano", job c0b1df92) succeeded + `ai.importGeneration` → tour advanced generate→listen; `transport.play` → shape; tempo nudge 92 BPM → mix (`editPerformed` classified); `track.setVolume` 0.8 → export (`mixerAdjusted` classified); `render.bounce` of the whole song → done, artifact verified ON DISK at `~/Library/Application Support/DAWPro/Generations/gate-ob-c-first-song/first-song.wav` (11,791,224 bytes ≈ exactly 30 s stereo float WAV); Finish → `completed`. Part B: app killed + relaunched — `completed` PERSISTED and the first-launch trigger did NOT re-offer. GATE-A 15/15 + part B 1/1. Environment restored: sidecar stopped (pre-gate dormant state), all app instances killed. Framing CTAs (Start/Finish) driven via debug.onboardingState state-sets — their plain-button UI was pixel-proven in the ob-b captures; every signal-bearing step advanced through the REAL observing adapter

## M9 — Performance, stability, release
- [x] Latency/CPU profiling passes; render-thread audit (no allocs/locks) — **PARENT COMPLETE 2026-07-10** (all four sub-items landed and independently verified: audit clean 0 violations → telemetry seam `engine.performanceStats` → load pass NO hotspot at v0 scale w/ release baselines + two real defects found → counter exactness under concurrency + saturation detector). Split 2026-07-10 (audit first: correctness before speed; then a measurement seam so profiling is wire-visible and repeatable; then the passes themselves):
  - [x] (perf-a) Render-thread safety audit — **COMPLETE 2026-07-10 (clean: 0 violations)**: 47 sites audited across 8 families (17 render-callback sites incl. all 5 instruments + hosted-AU adapters + ChainHostAU + 9 built-in effects + PDC ring + automation stages; 4 tap closures; 8 MIDI incl. the CoreMIDI receive block + LiveEventRing; vm-a analyzer re-verified — preallocation contract intact, snapshot() exception stands; stretch = worker-only confirmed; recording writer I/O confined to its serial queue; 7 CAtomics patterns — memory orders correct, no torn read feeds an audio decision; reachability sweep). Full inventory w/ per-site file:line/thread/verdict/rationale at `docs/research/audit-rt-safety.md`; **6 WATCH items** (fragile-but-sound: HostedAUEffect.reset() edge-triggered ObjC on RT, writer's tap-queue unfair lock, single-producer MIDI counters, CompensationDelay (de)alloc host guarantee, takeUnretainedValue borrow pattern, and DEAD `PlaybackGraph.detachAll()` — zero callers, exactly the M4 (i) segfault shape if ever wired live). ARCHITECTURE.md gained the "RT-safety invariants" section (two tiers, the two sanctioned state-crossing shapes incl. the pointer-publish/retire-bin idiom, complete 4-item exception list, boundary discipline) — the review reference for all future engine work. Teardown-crash leads recorded (observation only, per scope): renderer deinit releases its slot object with NO retire bin — safe only under AVFoundation's detach-synchronizes-with-render guarantee; first place to look when the parked project.new crash is picked up. Docs-only cycle (find-verified: zero source/test/mcp edits); verified independently: build 0 warnings, suite **1416/168 holds exactly**, findings doc spot-checked against the vm-a code (every claim grounded)
  - [x] (perf-b) Engine performance telemetry — **COMPLETE 2026-07-10**: `EnginePerformanceContext` (Sources/DAWEngine/EnginePerformance.swift) — 9 `daw_atomic_u64` cells in ONE heap allocation (CAtomics stable-address idiom; CAtomics.h gained u64 load-acquire/store-release), stamped per render callback by BOTH Tier-1 workhorses: `InstrumentRenderer.renderQuantum` (entry :198, exit on ALL THREE return paths via `@inline(__always) recordPerformance` :394) and `ChainHostAU.internalRenderBlock` (entry deliberately AFTER `pullInputBlock` returns :183 so nested upstream renders are never double-counted; error returns uncounted; wired via `setPerformanceContext` at node creation — PerformanceBox under the Scratch write-window discipline). Render side = integer math + ONE non-trapping Double (recentLoad EMA α=0.0106 ≈ 1−exp(−1/93.75), ~1 s at 512/48 k per block; NaN/denormal flushed); timebase numer/denom precomputed at init; all divisors guarded ≥1; increments = sanctioned single-producer load→store RMW (**invariants exception 5 added** — live graph + offline render may overlap; worst case one lost increment, never a torn value, telemetry-only). `overrunCount` = budget-overrun PROXY (elapsed > frames/rate strictly), documented ≠ CoreAudio xrun. RESET = reader-side baselines over lifetime-monotonic cells — closing/fresh windows tile exactly (straddling callback lands in exactly one window; naive cell-zeroing could resurrect totals); only ballistic peak/EMA cells zeroed (benign-race analysis in the header). Ownership: AudioEngine owns ONE context → hands to PlaybackGraph (:176) AND every renderOffline (:738) — offline bounces count (= the headless E2E seam). Wire: `EnginePerformanceStats` (Model.swift:1093, 10 fields, all-finite contract, `.idle`) → protocol + default → store passthrough → `engine.performanceStats {reset?}` (Commands.swift:643; allCommands 94→**95**; NEW `engine.*` namespace in ARCHITECTURE list) → `engine_performance_stats` tool (server.ts:2368; pin 97→**98**) + reference-grade ARCHITECTURE deep-dive row. +16 tests +3 suites (exact synthetic-timebase counter/EMA/overrun/reset-tiling math, degenerate guards, headless offline E2E — 24 callbacks/96,000 frames for a 1 s render — Codable/idle/passthrough, command shape/reset/param tolerance). Three labeled deviations, all verified sound (accumulated budgetNs cell for mixed-rate windows; baseline reset > cell-zeroing; per-engine context for test determinism + honest semantics). Verified independently: build 0 warnings, suite **1432/171** (from 1416/168), npm 19/19 at 95/98, isolation clean (no DAWApp/DAWAppKit/AIServices/scripts edits), code review of context + both hooks against the invariants section passed, and a **14/14 live wire session** (17695): fresh-session all-zero shape → pop skeleton → play ≈2 s → 940 callbacks/481,280 frames/avg load 6.8 % at 48 k/512 → counters strictly increasing between polls → reset returned the closing window (1270) and the fresh window read 0 with peak rearmed → post-stop counters stay monotone+readable (live-thru pulls keep rendering — by design, not a freeze), 0 overruns, unknown-param tolerant
  - [x] (perf-c) Load profiling pass — **COMPLETE 2026-07-10 (orchestrator-run measurement campaign; verdict: NO engine DSP hotspot at v0 scale — no optimization warranted, and forcing one would have been dishonest)**: full findings at `docs/research/perf-c-load-profile.md`. RELEASE-build numbers (the baseline the telemetry seam now defends): 41-track session (37 dense saw polySynths — 256-note clips, two-note stacks per 16th — preset FX chains on every track, 3 FX buses w/ sends from every synth, 2×64-pt automation lanes/synth) plays at **≈14 % of one core** total, per-callback peak **0.9 % of the 10.67 ms budget**, **0 overruns** anywhere; 30 s offline mixdown = **22.9× realtime**. First before/after = debug vs release: ~28× per-block load delta, ~80× offline throughput delta (0.29×→22.9× RT) — **all future perf measurement on release builds, always**. Method contributions (wire-visible, reusable): expected-callback-rate check `blocks × deviceRate` (device pulls at ITS rate — 44.1 kHz/512 = 86.13/s per block, measured exact at N=1); one-block-per-instrument-track (insert chains render INLINE in renderQuantum per the M4 ii design — ChainHostAU = audio/bus strips only); offline renders in 4096-frame slices (net-of-live-thru arithmetic proves all ~40 blocks render offline, counted correctly); stopped ≠ idle (live-thru pulls every node — subtract stopped-rate from offline windows). TWO REAL DEFECTS FOUND, neither in DSP: (1) telemetry undercount under AVAudioEngine's parallel render pool → split to (perf-d); (2) the parked teardown crash REPRODUCED with a full symbolicated stack → `docs/research/repro-teardown-crash.md`, feeds the crash-safety item. Campaign scripts preserved in session scratchpad (profile-perf-c.mjs / experiment-blocks.mjs / diagnose-perf-c.mjs)
  - [x] (perf-d) Telemetry exactness under concurrent producers — **COMPLETE 2026-07-10 (with a finding that CORRECTED perf-c's attribution)**: CAtomics.h gained `daw_atomic_u64_add` (fetch_add acq_rel — one LDADD on arm64, returns prior) + `daw_atomic_u64_max` (bounded CAS-max; boundedness argument in the header: retries ≤ concurrently racing writers, progress global); `EnginePerformanceContext.bump()` → fetch_add for the five monotone accumulators, peak → CAS-max; EMA + last-seen cells stay racy-benign BY DESIGN (comments now warn against "fixing" them). ARCHITECTURE invariants: multi-producer accumulator promoted to the THIRD sanctioned state-crossing shape; exception 5 shrunk to the racy-benign cells. Regression pin: 8-thread × 20k-record contention test asserting EXACT counts/sums/peak — **empirically falsified by the agent** (temporarily reverted bump() → test caught 45 % lost increments, the defect's exact shape; restored, deterministic-pass 0.13 s). THE FINDING: post-fix debug@16 tracks still reads ~597/s (pre/post identical) ⇒ those are REAL dropped device IO cycles (debug saturation — CoreAudio skips late cycles), NOT lost increments; live graph proven single-threaded (perf-b's premise held for pure live playback), and the true multi-producer overlap = live render thread + concurrent offline renders (in-block time > wall, proven in perf-c's offline windows). perf-c doc gained the correction addendum; the expected-rate check is hereby a wire-visible SATURATION DETECTOR (measured < blocks × device-rate = dropped cycles = glitch territory; debug saturates ≈16 dense tracks, release exact at every scale tested). Verified independently: suite **1433/171** (from 1432), isolation exactly 4 files (CAtomics.h/EnginePerformance.swift/tests/ARCHITECTURE.md), ops code-reviewed textbook-correct, wire re-runs on BOTH builds (debug reattribution + release no-regression: 87→1394/s exact scaling, loads unchanged). npm untouched (no wire change; 95/98 pins stand)
- [x] Crash safety — **PARENT COMPLETE 2026-07-10** (all three sub-items landed and independently verified: teardown UAF root-caused + retire-bin fix → rolling autosave + honest crash detection + recovery offer in-app and on the wire → heartbeat watchdog driving the proven recovery path). Split 2026-07-10 (fix the known killer first; then recovery; then detection):
  - [x] (crash-a) Teardown crash: root-cause + fix + regression guard — **FIXED 2026-07-10 (root cause PROVEN with a 6-experiment live evidence matrix, not conjectured)**: mechanism = AVFoundation completes a node detach's internal graph bookkeeping ONLY when it can synchronize with a running engine; detaching a once-rendered node while STOPPED leaves a stale raw `AUGraphNodeBaseV3*` in `AVAudioEngineGraph`'s internal list (survives `reset()`+`start()`; explicit disconnect-first does NOT purge it — experiments D1/D2), ARC then frees the node, and the next live `connect` (first track.add after project.new) walks freed memory. Our teardown hit it because reconcile's removal pass fires the quiesce-STOP at the first non-trivial strip, so the mass detach ran stopped-after-running. KEY INVERSION (exp C): stop-before-any-detach — the "cleanest" reading of the old discipline — still crashes; the discipline's stop IS the poison for detaches (safe direction is attach-while-stopped/cold-start; detach needs RUNNING — exp B/E clean incl. a full 28-strip live teardown). H2 refuted (every removed node had a paired detach); H3 not the trigger but the fix restores the detach-sync guarantee the renderer-deinit WATCH item leans on. FIX (minimal): `PlaybackGraph.retireNode(_:)`/`flushRetiredNodes()` + `engineHasRun` — running or never-ran → detach now (offline/test byte-identical); stopped-after-running → park attached+strongly-held in a FIFO pending-detach bin (no freed-memory window CAN exist), flushed after every successful `engine.start()` (prepare/rewire-bounce/config-recovery) + defensively at reconcile entry; every teardown detach site seam-routed incl. dead `detachAll()`; announce/bounce semantics untouched; silently covers latent same-class windows (live addSend old-gain teardown, invalidateInstrumentNode, mixed bus-removal passes). Root-cause note + evidence matrix: `docs/research/fix-teardown-crash.md` (written BEFORE the fix); repro doc status FIXED; ARCHITECTURE boundary discipline gained the lifetime rule. Regression: `TeardownRetireTests.swift` — 4 deterministic ordering pins (live crash is HAL-timing/heap-reuse dependent, unreachable headless; labeled as such). Verified: agent live 3× full-repro survived + −23.07 dB sanity; ORCHESTRATOR independent gate 5/5 — 3 consecutive teardown cycles in ONE process (16 synths + 3 buses + sends each, played) + 5 adds each + post-churn playback −24.04 dB + undo intact + ZERO new .ips; suite **1437/172** (from 1433/171) independently; isolation exactly 5 files, zero instrumentation remnants; 0 warnings. Agent: audio-dsp-engineer, ~35 min/122 calls, 5 fresh .ips banked during discrimination, zero deviations
  - [x] (crash-b) Autosave + crash recovery — **COMPLETE 2026-07-10**: headless `AutosaveManager` (`Sources/DAWCore/AutosaveManager.swift`) keeps ONE rolling `autosave.dawproject` (existing bundle serialization VERBATIM — absolute media refs, zero copies, never the user's file) + `manifest.json` `{savedAt, sourcePath?, editCount}` + `session.lock` `{pid, startedAt}` under `~/Library/Application Support/DAWPro/Autosave/`, injected dir + clock (the PanelDensity/OnboardingStateBacking idiom). `ProjectStore.autosaveTick()` snapshots off the edit path — gated on DIRTY + not-recording + a NEW journaled edit since the last write (the existing `lastEditEvent.seq` high-water mark, NOT a parallel dirty flag — quiet tick rewrites nothing, "2 ticks = 1 file"); ENCODE on the main actor, WRITE off it (detached task over the `Sendable` `ProjectDocument`) so zero edit-path latency. Crash detection: `beginCrashDetection()` latches a surviving prior lock BEFORE writing this session's lock; clean exit removes it via `NSApplication.willTerminateNotification` — fires ONLY on an in-app quit (Cmd-Q / the quit Apple event); SIGTERM/pkill does NOT route through AppKit termination (orchestrator gate falsified the brief's assumption live) and correctly reads as a crash, since nothing saves on the way down. Wire pair `project.recoveryStatus` (→ `AutosaveRecoveryStatus {available, savedAt?, sourcePath?, editCount?}`, readable anytime, headless-safe `{available:false}`) + `project.recover {accept}` (true → load snapshot as the live session, kept DIRTY with `projectPath` restored, then invalidate → `{recovered, warnings, snapshot}`; false → `{discarded}`; none available → `noRecoveryAvailable`); manual save/new/open all invalidate (gated no-op unless the manager engaged, so headless saves never touch the real profile). In-app: neutral-glass `RecoveryOfferView` (CYAN accent, NO violet — app chrome not AI; RESTORE/DISCARD → the SAME store path the command uses), staged by app-tier `debug.recoveryOffer {show?}` (off allCommands/MCP, the `debug.onboardingState` precedent). Supersedes the legacy in-place `startAutosave`/`autosaveIfNeeded` (kept + still tested, no longer app-wired). MCP: `project_recovery_status` + `project_recover` (98→**100** tools, allCommands 95→**97**). **1456 tests/174 suites** (from 1437/172, +19/+2: DAWCore `CrashRecoveryTests` 12, DAWControl `RecoveryCommandTests` 7), npm **19/19**, 0 warnings; sheet capture pixel-reviewed (cyan icon/AUTOSAVED readout/RESTORE, "Midnight Drive" project readout, no violet). ARCHITECTURE gained the command deep-dive row + updated persistence section. Deviations: (1) `startCrashAutosave` runs the app's 30 s tick loop (mirrors the existing `startAutosave` Task-loop idiom — keeps the timer out of the app, no self-capture); (2) the legacy in-place autosave is retired from the app (its silent save-in-place conflicts with the separate-snapshot crash model) but kept in DAWCore with its tests green rather than deleted (out-of-scope removal would shrink the suite). ORCHESTRATOR LIVE GATE (17695, real 30 s timer, real SIGKILL): crash→accept restores content under original IDs kept DIRTY ✓, crash→discard leaves a fresh session ✓, no legacy `Untitled-*.dawproj` accumulation ✓ — plus TWO real bugs found and fixed on the spot with regression tests (suite → **1458/174**): (a) an unresolved offer no longer gets overwritten by the live session's autosave tick (the rolling snapshot is the crashed session's ONLY copy; wire edits bypass the launch sheet — writer PARKS until the offer is resolved), (b) resolving/superseding the offer SPENDS the crash latch, so later autosaves can never re-arm `available:true` offering the live session's own snapshot as "crashed work". Follow-up parked: ~70 stale legacy `Untitled-*.dawproj` bundles accumulated in the Autosave dir from before this cycle (legacy writer, one per untitled session, never cleaned) — a one-time sweep belongs in a later hygiene pass. **Hygiene follow-up RESOLVED 2026-07-10** — root cause corrected on investigation: the accumulation is NOT the retired legacy writer but the still-live, intentional `flushForTransition()` (one recovery bundle per dirty untitled session abandoned via project.new/open — data safety, kept), with only same-slug cleanup existing, so foreign slugs grew unbounded (114 counted, fresh ones minted by that night's own gates). Fix: `ProjectStore.pruneUntitledRecoveryBundles(keep: 5)` — newest-by-mtime retention, prefix+extension matched only, never the live session's own bundle, best-effort deletes — wired at app launch after `startCrashAutosave()`; one-time backlog sweep 114 → 5 (session.lock preserved); +2 regression tests (retention/decoys/idempotence + missing-dir/under-cap edges) → suite **1520/184**; live-gated on 17695 (4 seeded over-cap fakes pruned at launch, exactly the newest 5 real bundles survive).
  - [x] (crash-c) Engine watchdog — **COMPLETE 2026-07-10**: `EngineWatchdog` (Sources/DAWEngine/EngineWatchdog.swift, @MainActor, tick-driven, injected heartbeat/isEngineRunning/restart — no wall clock) watches the perf-b LIFETIME callback cell (`EnginePerformanceContext.lifetimeCallbackCount()` :205, load-acquire only — deliberately NOT the windowed snapshot so a `reset:true` rebase can't fake movement): frozen ×2 ticks while running → `.recovering` + restart; success → `.ok`+restartCount; 3 consecutive failures → STICKY `.failed` (checked before the not-running→idle rule — the only reading honoring "never busy-retry"; only `prepare()`'s `reset()` re-arms; restartCount survives reset as lifetime self-heal history). Armed only when `isRunning && graph.hasInstrumentedStrips` (:341 census) — an EMPTY running session has no telemetry producer and would restart-thrash otherwise (agent catch, improves the brief). Loop: 2 s default injectable interval in AudioEngine (start in prepare :316, cancel in shutdown), ≈4 s worst-case detection; `watchdogRestart()` :1380 = engine.stop() (a silently-stalled engine may still claim running) → shared `recoverEngine()` → prepare-if-still-down. `recoverEngine()` :1309 = VERBATIM extraction of handleConfigurationChange (now a one-line alias :1296-1298, orchestrator-verified) — beats-from-anchor, crash-a `flushRetiredNodes()`, mix restore, resume; notification path byte-identical. Wire `engine.watchdogStatus` (Commands.swift :126/:667, allCommands 97→**98**, headless-safe idle/zeros) + MCP `engine_watchdog_status` (server.ts:2409; pin 100→**101**); `EngineWatchdogStatus` Model.swift:1161 + protocol default + store passthrough. Suite **1479/178** (from 1458/174: +21/+4 — state-machine ×9, engine-wiring ×4 incl. device-gated live restart E2E, Codable/protocol ×4, command ×4), npm **19/19**, 0 warnings. Orchestrator live gate on 17695: cold idle/zeros → playing ok heartbeat 189→564 → post-stop STILL advancing (955, live-thru law on the wire) → restartCount 0. Deviations: no `lastRestartAt` Date (JSONValue has no ISO strategy — count+state carry it); `AudioEngine.graph` private→internal for read-only test pins; offline-render heartbeat masking = documented detection delay, never false-positive. OPS NOTE: the implementing agent was WRONGLY killed at final verification by an orchestrator monitoring fault (persisted Bash cwd + suppressed stderr = false "zero writes"); resumed via SendMessage with state-on-disk, finished in 3 tool calls — laws recorded in memory.
- [ ] App bundle, signing, notarization, DMG; auto-update — split 2026-07-10 (prereq check: Xcode 26.6 ✓, but `security find-identity` = 0 valid codesigning identities and no notarytool profile → Developer-ID/notarization/auto-update are credential-blocked):
  - [x] (pkg-a) App bundle: `scripts/bundle.sh` — release build → `dist/DAWPro.app` (Info.plist with proper bundle id/version/NSMicrophoneUsageDescription/LSMinimumSystemVersion 14.0, ad-hoc codesign), LaunchServices-registered so quit-by-Apple-event works by name; wire round-trip gate on the bundled binary — **COMPLETE 2026-07-10**: `scripts/bundle.sh` (idempotent, `set -euo pipefail`) builds release → assembles `dist/DAWPro.app/Contents/{MacOS/DAWApp, Info.plist, Resources/, PkgInfo}` from the `scripts/Info.plist` template (version constants substituted; bundle id `dev.dawpro.app`, exec name kept `DAWApp` for pgrep continuity, `NSMicrophoneUsageDescription` + `NSHighResolutionCapable` + category `public.app-category.music`), `codesign --force --sign -` (adhoc), `lsregister -f`. No icon (glass-b key-blocked). `dist/` git-ignored. GATES: `codesign -dv` = adhoc/valid/satisfies-DR, `plutil -lint` OK (12 entries); wire round-trip on the BUNDLED binary at 17698 — `project.recover{accept:false}`→discarded, `track.add`→"Audio 1", `engine.watchdogStatus`→idle/engineRunning:false/ok-shape; Finder `open` + `osascript -e 'quit app "DAW Pro"'` exited cleanly and REMOVED `~/Library/Application Support/DAWPro/Autosave/session.lock` (crash-b willTerminate path, now drivable by name — the point of LS registration). Suite HELD **1479/178**, 0 warnings, ZERO Swift/Package.swift/mcp-server changes. Docs: new `docs/PACKAGING.md` (usage, bundle-id/UserDefaults-domain note — Finder bundle uses `dev.dawpro.app`, old bare-binary `DAWApp` prefs do NOT migrate, ACCEPTED; mic-permission note; sidecar `DAWPRO_ACESTEP_DIR` gap — dev-launch in `dist/` still walks up to Package.swift so it works, a COPIED bundle reads `notInstalled` without the env var, DOCUMENTED not coded; ad-hoc/Gatekeeper right-click-open caveat lifted by pkg-b/c)
  - [ ] (pkg-b) Developer ID signing + hardened runtime + entitlements — BLOCKED: no codesigning identity on this machine (see ## Blocked)
  - [ ] (pkg-c) Notarization + stapling — BLOCKED: no Apple ID / notarytool keychain profile (see ## Blocked)
  - [x] (pkg-d) DMG packaging — **COMPLETE 2026-07-10 (orchestrator-inline: ~30-line script, zero code changes)**: `scripts/dmg.sh` = bundle.sh (idempotent) → staging with `/Applications` symlink (drag-to-install layout) → `hdiutil create` UDZO → `dist/DAWPro-0.1.0.dmg` + SHA-256 (version read from the built bundle's Info.plist via PlistBuddy — single source of truth stays bundle.sh). Install-simulation gate PASSED: mount → layout (DAWPro.app + Applications symlink) → copy out → copied bundle's ad-hoc signature still `valid on disk`/`satisfies its Designated Requirement` → installed copy launches, honors DAW_CONTROL_PORT, full wire round-trip (recover-normalize, track.add, ai.sidecarStatus, watchdog idle-shape) → `quit app "DAW Pro"` clean exit + session.lock removed. Gate also caught a doc nuance (pinned in PACKAGING.md): the sidecar walk-up resolves from process CWD too — a copied bundle exec'd from a repo shell reads installedNotRunning; only Finder launch (cwd /) shows the pure installed-copy `notInstalled`. Suite untouched by construction (script + docs only).
  - [ ] (pkg-e) Auto-update (Sparkle or equivalent) — BLOCKED: requires real signing (pkg-b) + a hosting URL (see ## Blocked)
- [x] Beta feedback loop — **COMPLETE 2026-07-10**: headless `DiagnosticsReporter` (`Sources/DAWCore/DiagnosticsReporter.swift`, injected `outputDir`/`crashReportsDir`/`clock` — the `AutosaveManager` idiom) writes ONE local FOLDER `feedback-<timestamp>/` under `~/Library/Application Support/DAWPro/Feedback/` (NOT a zip — fewer failure modes, testable headless, Finder-attachable): `manifest.json` (`FeedbackManifest` — appVersion/build from `Bundle.main`, "dev" for a bare binary; osVersion + `hw.model` sysctl; createdAt; includesProject; crashReportCount), `engine.json` (the CURRENT `EngineWatchdogStatus` + `EnginePerformanceStats` — the M9 telemetry payoff, passed IN so the reporter stays engine-free), `overview.json` (the counts-only `ProjectOverview` — no note content, no media paths), a `crashes/` copy of recent `DAWApp*.ips` (14-day window, cap 10, foreign-process/wrong-ext ignored, no-dir tolerated), and — ONLY on opt-in — `project.dawproject/` via the SAME `buildAutosaveDocument()` serialization. NOTHING phones home; NO key material near the bundle. `ProjectStore.writeFeedbackBundle(includeProject:)` gathers overview + engine snapshots and drives it; wire `app.feedbackBundle {includeProject?}` (the first `app.*` command; allCommands 98→**99**, never throws except `DiagnosticsError.writeFailed`) → `FeedbackBundleSummary {path, fileCount, byteCount, crashReportCount, includesProject}`; MCP `app_feedback_bundle` (server.ts; pin 101→**102**, bijection auto-adjusts) teaches agents to run it on a bug report and to ask before `includeProject:true`. In-app: neutral-glass "Beta Feedback" row in Settings (CYAN, NOT violet — diagnostics not AI) — "Save Feedback Bundle" reveals the folder in Finder + echoes the path, INCLUDE PROJECT toggle (default off); the below-the-fold row is deep-linkable via `ui.showSettings {reveal:"beta"}` (app-tier, off allCommands/MCP). New `docs/BETA.md` (install/Gatekeeper right-click-open, first-launch prefs-domain/mic/sidecar notes, known gaps, how-to-report, privacy statement). Suite **1492/180** (from 1479/178: +13/+2 — DAWCore `DiagnosticsReporterTests` 8, DAWControl `FeedbackBundleCommandTests` 5), npm **19/19** at 99/102, 0 warnings. Settings Beta row pixel-reviewed (cyan Save button + ladybug, INCLUDE PROJECT checkbox off, no violet); live wire gate on 17698 (bundle written with 10 crash reports copied, opt-in project folder + same-second collision uniquified `-2`, key-material scan clean, engine snapshot threaded through). ARCHITECTURE gained the `app.feedbackBundle` deep-dive row.

## M10 — Beta feedback round 1 (first real-user session, 2026-07-11)

Triage of the beta user's questions, feature requests, and bugs. Bugs first, then UX affordances, then capability gaps. NOTE: the user has stored a real **Anthropic key in the Keychain** — the key-blocked M6/M8 gates (rail-e, Lyrics, ex-c) become runnable the moment (m10-a) lands.

- [x] (m10-a) **BUG (user-hit): provider response parsing** — **FIXED + LIVE-VERIFIED 2026-07-11**: `AnthropicClient.complete` now iterates ALL content blocks (collects every `text` block joined with newline, skips `thinking`/`redacted_thinking`/`tool_use`/unknown) and surfaces API error envelopes with the vendor's own `error.type: error.message` (never the misleading missing-text message); `HTTP.postJSON` non-2xx path formats vendor error bodies via shared helpers (SunoClient/ACEStepClient untouched); `OpenAIClient.chat` handles null content + `refusal` + array-of-parts; `CopilotProvider` both branches gained explicit thinking-tolerance + envelope surfacing (thinking already fell into `default: continue` — now explicit). +10 tests/+2 suites of stub-HTTP fixtures (thinking-first, joined texts, 529 envelope, no-text informative error, OpenAI refusal/parts, Copilot thinking+tool_use) → suite **1530/186**, 0 warnings. ORCHESTRATOR LIVE GATE (17695, real Anthropic API through the Keychain key — the user's exact failure path): `ai.providerStatus` shows anthropic/keychain/configured, `ai.writeLyrics` returned well-formed bracketed synthwave lyrics in 5.3 s, provider=anthropic, 7/7 checks. NOTE: the user's installed `dist/DAWPro.app` still runs the pre-fix binary — rebuild via `scripts/bundle.sh` once their instance isn't running, then relaunch.
- [x] (m10-b) **BUG: sidecar start resilience + progress UX** — **COMPLETE 2026-07-11** (launcher half: `run.sh` `uv run --no-sync` when `.venv` exists; UX/resilience half landed by mcp-integration-engineer). SECOND ROOT CAUSE fixed beyond the launcher: `startingSince` died with the blocking `start()` call, so after the 30 s window every `status()` poll misreported `.installedNotRunning` while models were still loading — now boot-long `startedAt` (cleared only on healthy/pid-death/stop) + pidfile-liveness fallback (mtime = boot start, survives app relaunch mid-boot) keeps `.starting` truthful, and a dead pid with a lingering pidfile reports "boot likely failed, check the log". NEW: `SidecarStartPhase.classify(logTail:)` (pure, markers pulled from the real ace-step.log) → additive `phase`/`startingForSeconds` on SidecarStatus flowing through ai.sidecarStatus/Start unchanged (zero mcp-server edits); Sketchpad banner `.progress` tone (cyan spinner + "Starting — ‹phase› (‹N›s)"), STARTING… in-progress button, poll loop tightens to 2 s while starting; `debug.sidecarSeed` staging (app-tier, off allCommands/MCP). +15 tests/+3 suites incl. a REAL spawned-process integration test of the exact beta path (start timeout → later status still starting) → suite **1545/189**. ORCHESTRATOR VERIFIED: build 0-warning, suite 1545/189 pass, live cold-boot gate 10/10 on 17695 (two-connection: start blocking on A, B polling — B saw truthful `starting` + "preparing environment…" + elapsed during boot, NEVER installedNotRunning; boot→healthy in 3.7 s post---no-sync; stop clean; stale-pidfile "boot likely failed" classification fired correctly on real leftover state), both banner captures pixel-reviewed (seeded starting spinner + real stopped-warning). Residual polish note: the installedNotRunning banner text says "call ai.sidecarStart" — wire-speak in a user-facing surface (pre-existing copy; fold into m10-q polish).
- [x] (m10-c) Hover cursor affordances — **COMPLETE 2026-07-11** (ui-design-engineer): every existing drag/resize surface now advertises controllability. Pure `CursorKind`/`CursorAffordance` catalog + clip/note zone mappings in `DAWAppKit/CursorModel.swift` (mirrors `ClipEditGeometry.classifyZone`/`PianoRollModel.hitTest`, 12 headless tests); thin glue `DAWApp/Components/HoverCursor.swift` — ONE `nsCursor(for:)` switch, `DragCursor` gesture helper (holds grabbing/resize for the WHOLE drag even outside bounds, clears on end), and a hit-test-transparent `NSTrackingArea` overlay (`set()` not push/pop → stuck-cursor impossible; resets on exit/teardown; coexists with Explain hover + all gestures). 11 adoption sites (master fader, channel fader, pan knob, send mini-fader, velocity stems, note grid resolver grab/resize, automation lane grab/crosshair, take-lane crosshair, clip-block zone resolver body-grab/trim-resize/gain-chip, ⌥-stretch, gain chip) — full file:line inventory in the DESIGN-LANGUAGE "Pointer affordances" section's cycle notes. Suite **1557/190** (+12/+1), 0 warnings; orchestrator verified build+suite, spot-checked clip+note resolvers mirror gesture hit-routing exactly, arrange+piano-roll captures layout-clean. HONEST LIMIT: cursor glyphs aren't rasterized in window captures — final feel is the beta user's hands (this item was their ask). SCOPE FLAG resolved: track headers have NO drag interaction today, so no cursor added there (would advertise a lie); header adjustability + its grab cursor land with (m10-d), which already names it. Convention deviation (pre-authorized, sp-b precedent): no wire/MCP surface — pointer feel isn't invokable.
- [x] (m10-d) Adjustable panel layout — **COMPLETE 2026-07-11** (ui-design-engineer, 2 rounds): `PanelLayoutStore` in DAWAppKit (@Observable, injected backing, UserDefaults keys `panelLayout.*`, clamping in the store, 12 tests) drives three dimensions — sidebar width (splitter between track list and timeline), editor height fraction (splitter on the editor's top edge), and GLOBAL track-row height (drag any header's bottom edge; idle-invisible grabber) — all with m10-c hover cursors (resizeLeftRight/resizeUpDown; "grab" rejected as the wrong macOS glyph for dividers, documented). `rowHeight` threaded through every lane-geometry site (ClipEditGeometry/laneTop/takes/automation/ClipBlock + sidebar rows — file:line inventory in the agent transcript); reusable `PanelSplitter` (hairline at rest, cyan glow hovered/dragging, ≥6 pt hit target); `debug.panelLayout` staging (app-tier, off allCommands/MCP). ROUND 2 (orchestrator gate finding): original clamps reached broken layouts (sidebar 180 → header content bled across the splitter + window edges clipped; editor 0.70 → app header + transport pushed off-screen) → agent MEASURED the header row's intrinsic min via NSHostingView.fittingSize (242–267 pt) → ranges tightened to **250–420 / 0.30–0.55 / 24–64**. RE-GATE: suite **1569/191** 0-warning ×2, wire matrix 8/8 (defaults echo, extremes apply, ceiling+floor clamps, reset), UserDefaults persistence on disk, captures pixel-reviewed: 250/0.55 keeps ALL chrome visible; rowHeight-64 header↔lane alignment pixel-exact. RESIDUALS (recorded, not blockers): (1) rowHeight 64 × several tracks can still push a sliver of the title row — static clamps can't be track-count-aware; real fix = (m10-j) scrollable arrange; (2) header-row graceful degradation (flexible MiniLevelBar) deferred to (m10-j) as measured-risky; (3) TWO NEW BUGS found during the gate, filed as (m10-s)/(m10-t) below. Convention deviation (panelDensity precedent): no user-facing wire/MCP surface.
- [x] (m10-e) Piano roll playhead — **COMPLETE 2026-07-11** (ui-design-engineer): headless `DAWAppKit/PianoRollPlayhead.swift` (localBeat/isVisible inclusive-edge/lineX nil-outside/scrubProjectBeat clamped-unsnapped; 5 tests → suite **1574/192**); glowing cyan line (identical idiom to arrange: 1.5 pt #3EE6FF glow r5) through note grid + velocity lane, rendered ONLY while `positionBeats` falls inside the edited clip's span (honest absence otherwise); scrub = frozen top strip pinned to the VISIBLE grid (labeled deviation — the literal grid-top is scrolled off-screen at middle-C anchor; strip is scroll-offset-corrected via GeometryReader), click-jump + free unsnapped drag → `store.seek`, m10-c resizeLeftRight cursor held through the drag; grid extracted into a tick-stable `PianoRollGrid` struct so transport ticks move only the playhead offset (no per-frame Canvas redraw). Position source = the SAME `store.transport.positionBeats` the arrange timeline consumes. ORCHESTRATOR GATE: build 0-warning, suite 1574/192, live captures with clip at NON-zero start (beat 4, len 8): inside (beat 8 → line exactly at the left edge of the local-beat-4 note — content-anchored proof), start-edge archived, outside (beat 20 → NO line, arrange playhead at bar 6) — 5/5 wire checks. Pure-view deviation (no new wire/MCP surface; transport.seek pre-exists) per the m10-c precedent.
- [ ] (m10-f) Clip content previews in arrange — audio clips render their waveform, MIDI clips a mini note map, so you can see where the sound sits (user reports clips look blank; verify current state first).
- [ ] (m10-g) Loop region mouse UI — mark start/end on the arrange ruler and loop it (wire `transport.setLoop` exists; this is the missing direct-manipulation surface).
- [ ] (m10-h) Piano roll time-range editing — delete/insert bars (remove a measure and close the gap).
- [ ] (m10-i) Track header UX — titles always visible, double-click to rename (wire `track.rename` exists, UI lacks it), discoverable add-track affordance for humans (user asked "or is it AI-only?" — it must obviously not be).
- [ ] (m10-j) Window layout floors — minimum window size + scrollable main content; panels must never push the title bar/chrome offscreen when the window shrinks.
- [ ] (m10-k) Import your own audio/stems/vocals from the UI — File → Import + drag-and-drop onto arrange (backend `clip.addAudio` import path exists; wire/AI-only today).
- [ ] (m10-l) Control/MCP connection UX — Settings row showing the live ws URL (`ws://127.0.0.1:17600`) + in-app port configuration (persisted; today only the `DAW_CONTROL_PORT` env var), plus the USER-GUIDE section documenting agent hookup.
- [ ] (m10-m) Copilot limits configurable — max rounds/tool calls per turn user-settable (hardcoded 8 today), surfaced in Settings + honored by `ai.copilotSend`.
- [ ] (m10-n) Instrument library UX ("instrument sets") — a real instrument picker listing installed AU instruments (`instrument.listAudioUnits` exists), plus SoundFont/DLS bank loading via AUSampler so MIDI tracks aren't stuck on built-ins. (AU hosting itself already works — this is discoverability + banks.)
- [ ] (m10-o) mcp-server: migrate hand-rolled Anthropic/OpenAI HTTP calls in `src/ai.ts` to the official `@anthropic-ai/sdk` / `openai` SDKs (user directive: standard SDKs wherever they exist).
- [ ] (m10-p) Vocals → voice-conversion pipeline — vocals-only generation, voice conversion to a target voice, optional on-device voice training. **RESEARCH DELIVERED 2026-07-11** (docs/research/2026-07-11-vocals-voice-conversion.md); recommendation: (1) vocals-only = reuse the existing extract/`track_name=vocals` stem path (ACE-Step has NO vocals-only task type; "a cappella" tags in genres_vocab are an unproven lever); (2) VC engine = RVC/Applio family (MIT, strongest singing-voice reputation; pure-MLX inference fork Acelogic/Retrieval-based-Voice-Conversion-MLX exists, inference-only) — seed-vc (GPL-3.0, archived) and so-vits-svc (AGPL, archived) rejected on license, DDSP-SVC (MIT) as fallback; (3) own-voice training = PyTorch-MPS only (no MLX training path), ~10–30 min clean dry vocals, M1 datapoint 6+ h → run a timing spike on this M5 before promising in-app training; (4) architecture = second loopback sidecar 127.0.0.1:8002 mirroring SidecarManager; (5) policy = your-own-voice only, never celebrity models, MIT-licensed defaults. Implementation splits (per research doc): spike → install → client → wire+MCP → UI → real-audio gate.
- [ ] (m10-q) Pro-parity audit — honest gap list vs Logic/Live for the features a pro expects that the user didn't explicitly ask for: session markers, tempo track + time-signature changes, count-in/pre-roll, track freeze/bounce-in-place, sidechain routing, MIDI CC lanes, groove-template UI on top of the existing groove engine, undo-history panel, per-clip gain envelopes UI, crossfade tool. Output = prioritized M11 candidates.
- [ ] (m10-s) **BUG (found in m10-d gate): recovery sheet ignores wire resolution** — `RecoveryOfferView` stays on screen after `project.recover {accept:false}` resolves the offer over the control WebSocket (screenshot-proven: sheet visible through an entire post-discard capture session). The sheet's presentation state must observe the store's offer state, not just its own button actions. Small fix + a staging-path regression check.
- [ ] (m10-t) **BUG (found in m10-d gate): app startup blocks on a synchronous Keychain read** — `AppModel.init` → `SettingsModel.init` → `refresh()` → `resolveKey` hits `SecItemCopyMatching` BEFORE the window exists; when macOS holds a consent prompt (any rebuilt/different-identity binary touching the stored key), the app hangs with no UI indefinitely (sampled: main thread parked in SettingsModel.refresh, control server never bound). Fix: defer/async the initial provider-status refresh until after the window appears, and use a fail-fast presence probe (kSecUseAuthenticationUI=skip) for status display — the interactive prompt should only fire on an actual key USE. Note: after any `scripts/bundle.sh` rebuild, the user's first launch may hit this too.
- [x] (m10-r) User guide + feature-set documentation — **COMPLETE 2026-07-11**: `docs/USER-GUIDE.md` (~450 lines: install both paths incl. Gatekeeper right-click-open, workspace tour, tracks/clips/takes/MIDI/mixing/loop/AI features/exporting, the "connect an AI agent over MCP" walkthrough — `ws://127.0.0.1:17600`, `DAW_CONTROL_PORT` override, mcp-server build + stdio registration — troubleshooting incl. sidecar log path, shortcuts) + `docs/FEATURES.md` (~200-entry matrix by domain with ✅/🔶/❌/🔒 status tags and an honest Limitations & WIP table: track-rename and loop-region wire-only, no MIDI CC/learn, linear-only automation, no master FX chain, notarization blocked) + README doc links. Orchestrator-verified: spot-checked highest-risk claims against ground truth (AU vendor-view wording correct, MCP section exact, command count 102 from allCommands); docs-only cycle, suite untouched by construction.

## Blocked

- ~~M4 (i) live mid-play send activation~~ **RESOLVED 2026-07-06** (diagnostic round 3, per the parked method: stderr instrumentation + node-connection dump + preallocated-atomic tap probes + the live `diag-send.mjs` repro). Root cause was NOT the send wiring: the connection dump immediately after the rewire resume showed the mid-play-added leg fully attached and connected (source mixer → gain(vol 0.5) → bus sum → chain host → bus mixer, 48 kHz/2ch throughout) and the gain node's tap FIRING — carrying peak 0.0. Two stacked mechanisms produced the "silent until stop/play" symptom, on ALL strips (bus A metered 0.0000 after the resume too — the old E2E's poll windows just never caught it): (1) **THE BUG** — the resume anchored `derivedBeats` on `outputNode.lastRenderTime`, which right after the stop→reset→start bounce still reports the PREVIOUS render session's sample clock (valid flags set, measured sample=98304) while the restarted session's clock begins again near 0; the `max()` clamp then froze the playhead at the resume beat for the ENTIRE prior session length (unbounded in a long session — why it read as "silent forever"), postponing the loop-wrap revival past every poll (predicted wrap +4.19 s vs observed +4.17 s, 36 ms match). (2) The repro material (7.5-beat sustained MIDI chord) has no note-on between the resume beat and the wrap, and no-chase is the settled v0 contract (`MIDIEventSchedule`, ARCHITECTURE.md), so the resumed schedule was legitimately silent until the wrap — "stop/play revives it" was simply a restart from beat 0 with fresh note-ons, not a topology repair. FIX (one flag): the rewire resume calls `startPlayers(…, renderClockTrusted: false)`, forcing the already-proven host-clock anchor branch (host time is monotonic across the bounce; players start on hostTime anchors in both branches, so nothing else changes). Live post-fix: MIDI pad send meters on B at +2.29 s (= the wrap, level exactly matching the stop/play reference, no transport command, A uninterrupted-then-revived per the restart contract); audio-clip send meters at +0.26 s with A continuously intact; bus E2E 19/19 (fx-poll window widened to cover the no-chase wrap, with comment); suite 920/117 green; app stderr clean.
- **Teardown crash: `project.new` after a played send/bus project** — discovered 2026-07-06 during M4 (i) verification; a SEPARATE pre-existing defect, independent of the send fix (the crash repro session never executes the rewire-resume path — all routing edits happen while stopped). Repro (`scratchpad crash-teardown.mjs`): build track→busA output + send→busB, `transport.play` ~1 s then `stop`, `project.new`, then `track.add` → SIGSEGV inside `-[AVAudioEngine connect:to:format:]` → `AVAudioEngineGraph::UpdateGraphAfterReconfig` → `swift_unknownObjectRelease` on a garbage pointer (`DAWApp-2026-07-06-051956.ips`; an earlier identical-trigger run instead crashed in a SwiftUI executor check, `…-051559.ips` — two surface stacks = heap corruption). The never-played variant does NOT crash (engine never ran → teardown against a stopped engine is safe). Suspect: the whole-project teardown (all source tracks + send gains + buses removed in one reconcile around the engine bounce) leaves a dangling node reference in the AVFoundation graph that the NEXT connect releases — same family as the round-1 live-bus-removal segfault. Prior E2Es never issued another graph mutation after a send-project `project.new`, so it went unseen (e2e-bus-test ends on `project.new`).
- ~~AUv3 plugin view hosting (M3 vi-b)~~ **RESOLVED 2026-07-10.** Unparked once pkg-a shipped the real bundle; landed as vi-b-1 (plumbing + generic body) and vi-b-2 (vendor-view ladder) — see the M3 checkboxes and `docs/research/design-vi-b-au-windows.md`. Residual, recorded honestly: no AUv3 exists on this machine, so the out-of-process AUv3 custom-view leg is untested by necessity (SKIPPED, never faked); the ladder degrades to the generic body + warning by design if one ever fails to serve a view.
- ~~Input device selection: pinned-device capture yields empty takes~~ **RESOLVED 2026-07-05.** Root cause was NOT the eager pin wedging the AUHAL (disproved by CLI repro: eager prepare + pin still delivers tap buffers). Two stacked bugs: (1) the same-device guard compared the pin target against the AUHAL's `CurrentDevice`, which on a fresh AVAudioEngine is the engine's PRIVATE AGGREGATE device — never equal to any raw hardware ID — so every pinned take (default uid included) performed a real device set; (2) that set makes AVFoundation post `AVAudioEngineConfigurationChange` on the capture engine ~65 ms after start with the engine still running and the tap still healthy, and the config-change handler treated it as fatal → `stopRecording()` finalized the writer before the first buffer, and the writer's post-finalize guard silently swallowed every later delivery (hence zero frames AND zero stderr). Fix: pin skips when the uid resolves to the current system default (`InputDevices.defaultInputDeviceID()`); config-change handler only ends the take when the capture engine actually stopped; two-phase `InputCapture.prepare()/start(writer:)` replaces the eager pin; 1.5 s first-buffer watchdog aborts loudly ("no audio arrived from input device …") instead of a silent empty take. Live E2E green: default-uid pin, ZoomAudioDevice pin (frames flowed), unpinned.
- ~~Lyrics workshop real-key gate~~ **RESOLVED 2026-07-11**: the (m10-a) parser fix landed and the orchestrator ran the exact unblock condition — one real `ai.writeLyrics` (staging 17695, Keychain Anthropic key, 5.3 s, provider=anthropic) returned well-formed bracketed [verse]/[chorus] lyrics, eyeballed, 7/7 checks → M6 Lyrics workshop box CHECKED. rail-e and ex-c are now the next runnable gates; glass-b still waits on an OpenAI key. Original entry — noted 2026-07-10: no Anthropic/OpenAI API key is present on this machine (neither env nor Keychain), so the workshop lands STUB-VERIFIED only (request shapes pinned against stub servers; full vertical tested with fakes) and its box stays unchecked until a real key exists. To unblock: paste a key in Settings (⌘, — Keychain-backed, added 2026-07-10) or set `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`, then run one real `ai.writeLyrics` and check the output is well-formed bracketed lyrics. Same blocker applies to the copilot rail's provider calls later in M6. **UPDATE 2026-07-11: an Anthropic key is NOW present in the Keychain** (confirmed by the beta user's real provider call, which surfaced the (m10-a) parser bug) — this gate, rail-e, and ex-c become runnable as soon as (m10-a) lands. glass-b still waits on an OpenAI key.
- **M8 (glass-b) GPT Image asset set** — noted 2026-07-10: GPT Image generation requires `OPENAI_API_KEY` (absent; same key gap as the Lyrics/copilot gates). The design-compliance half proceeded as (glass-a). To unblock: provide the key, then scope which surfaces actually want generated assets (the Canvas-drawn cockpit may need none — evaluate before generating).
- ~~M8 (ex-c) explain→copilot live hand-off gate~~ **RESOLVED 2026-07-11**: gate passed 7/7 (see the ex-c checkbox for evidence) — coherent punch-recording conversation through the rail, zero edits, zero key material. Original entry — noted 2026-07-10 at ex-a close-out: the "Ask the Copilot →" hand-off itself is key-independent (it only prefills the rail's input draft — verified live), but the (ex-c) gate — one real "explain this control" conversation through the rail — needs the same `ANTHROPIC_API_KEY`/`OPENAI_API_KEY` as rail-e. To unblock: provide a key, toggle EXPLAIN, hover a control, press "Ask the Copilot →", send the prefilled draft, and verify the rail answers coherently about that control without driving unintended edits; then check (ex-c).
- **M9 (pkg-b/c/e) Developer ID signing, notarization, auto-update** — noted 2026-07-10 at the packaging split: `security find-identity -v -p codesigning` returns 0 valid identities and there is no `notarytool` keychain profile, so Developer ID signing (pkg-b), notarization/stapling (pkg-c), and auto-update distribution (pkg-e, needs pkg-b plus a hosting URL) cannot proceed on this machine. Ad-hoc-signed bundle (pkg-a) + DMG (pkg-d) proceed now — sufficient for on-machine beta; distribution to OTHER machines hits Gatekeeper (right-click-open or `xattr -dr com.apple.quarantine`, documented in pkg-d). To unblock: enroll an Apple Developer account, install a "Developer ID Application" certificate, `xcrun notarytool store-credentials`; then pkg-b/c are mechanical re-runs of the same scripts with a real identity.
